# Log

Session narratives, newest first — what each stretch did and found, in full. The working queue lives on [`board.md`](board.md); this file is the history it stands on. (Formerly board.md §Recent.)

---

**The sweep read twice, and the method kept (2026-08-28 → 29).** The research re-derivation queued on the board ran as the code's first proof of concept. The session measured the raw sweep (45 reports, ~150k tokens) and — on the author's ruling that blank subagents lose exactly the connective tissue a compression needs — read all of it whole in one context, inventoried per report, outlined, and wrote [`spec/research/sweep-2026-08/findings/`](spec/research/sweep-2026-08/findings/README.md): an entry and twelve folds cut by subject rather than by run, each opening with what it settles and closing with what could not be established, confidence carried in the sentences — ~20k tokens, a 7:1 purification. The author's verdict became the session's hinge: a real improvement, essentially the code's law at work — *"but it's been folded categorically; there isn't meaning."* The request behind the verdict — be brought into contact with other people's work so that meaning compounds — made the code's open question on audience and outcome concrete: same corpus, two outcomes, and the cut that serves one flattens the other. Two rulings followed. The engaging piece must be written by a fresh head with no project context, so the verdict framing cannot contaminate the voice; and — the author's insight — *a link to an unknown still allows folds*: the piece ships complete with unopened doors, and later sessions open them one at a time. A purity-bootstrapped session (code.md, the sweep README, and raw/ only — no project tree, no findings/, no web) then wrote [`spec/research/sweep-2026-08/encounters.md`](spec/research/sweep-2026-08/encounters.md): a walk in ten movements among the people and works the sweep found — the structure-editor builders' own obituaries, Dynamicland's inversion, the lab in the middle, the mathematics that said no, the folklore tracers — ~9k tokens, 23 doors standing, ending on the two unclaimed things: the enclosure argument and the unrun crux experiment. Before closing, the author called for the method itself: a forensic pass over the sweep session's transcript recovered how raw/ came about — the single commission verbatim, the cleanroom briefs and their fixed skeleton, the spawn record and spend-limit interruption, the harvest command that kept 45 of 57 transcripts — and [`spec/research/sweep-2026-08/method.md`](spec/research/sweep-2026-08/method.md) now carries the chain of custody for all three artifacts, the encounters bootstrap prompt verbatim, and what the chain teaches. Its last lesson is self-demonstrating: stage 1's provenance survived only because the transcript still existed two days later — record the method at the moment of the work. All three readings stand as research register, awaiting the author's read; the code's *shape in practice* open now has its first observations.

---

**The code rebuilt from its essence (2026-08-26 → 28, three stretches).** The research sweep ran first (2026-08-26): a thirteen-agent fan-out across the knowledge landscape, raw reports kept whole in [`spec/research/sweep-2026-08/`](spec/research/sweep-2026-08/), compressed into [`spec/research/landscape.md`](spec/research/landscape.md) and an `evidence.md` that scored the findings against the code's then-current rules. The author then read the drafted code whole and filled a feedback file — folk-comprehensible English demanded, fold-as-file rejected, the project's goals placed beside the reader's, nothing ever finally settled — and the stewarding session, its window full, revised `code.md`, then reached for a handoff and three blind sub-agent drafts; the material carried, the voice did not. A fresh session (2026-08-28) cut through instead: the code collapses to one limit, two primitives, two laws — and an author-in-session rebuild replaced `code.md` whole: why → limit → fold and link → two laws → open. En route: the fold re-derived from the motion of work itself — a dive folded so its conclusion meets the thread exactly where it left off — retiring the map analogy; law 1, *nothing arrives before its ground* (prose running ahead is algebra: K, L and M about an unknown X; borrowing allowed, silent borrowing not); law 2, *the ground stays true* (one home per fact, confidence in the writing, settled never meaning forever ruled); "fold" and "code" both held as leans, *elements* weighed as the historical alternative. The document went medium-agnostic — the markdown practice moves to a first-proof companion piece, owed, its empty link standing in `code.md`. Cleanup for the next pass: the drafts and handoff consumed; `evidence.md` retired to git (its frame was the superseded rules, its stewarding context lost) and `feedback.md` retired as served; the raw sweep and `landscape.md` kept as the ore the re-derivation reads through the new code. Method finding, third time evidenced: the fresh head cut in minutes what loaded contexts circled for a day.

**The knowledge arc opened (2026-08-23, closing the sitting).** The session's unpacking reprocessed into [`spec/research/knowledge/opening.md`](spec/research/knowledge/opening.md) — the author's ground (governance and the cost of unmediated insight; Unix and the internet as medium-plus-join; night as the third move; the felt monolith of the only-growing context; the honest verdict that the code is not cracked), the state of the search across both projects, the sitting's additions (the values→methods gradient, the candidate code as steward's take, the compression finding, the three artifacts), and the grail named: the successor of conventions.md, the spec tree evolved, the proof being the reading experience itself.

**The board turnover (2026-08-23, same sitting).** The author called it: the board was unreadable to a human — a steward file throughout, its *Now* section a second log (landed events restated in compression beside `log.md`, against *records are events*), its *Ruled* and *Namings* sections records beside the law they produced. Rewritten whole as pure state: where we are, the queue, the gates, the debt, notes — written for a human first. Retired to git, with their content verified to stand elsewhere before retirement: the *Now* stack (every entry's narrative already in this log), the *Ruled — closing stretch* list (folded into the specs during the fold sessions; narratives here), the closed *Gates* rows (typed bodies, agent-contexts, the small-rulings roll-up — all folded), and *Namings — pinned for code* (historical since the rewrite; the spec tree is the naming authority, and the supersession list lives in git). Left out knowingly: nothing else — the queue, standing gates, debt and notes carried whole.

**The entry rewritten by the code, and the blind-variant method tried (2026-08-23, same sitting).** The author turned the sitting from ratification to the knowledge-structure search — the wider why unpacked live (compounding by interconnection; Unix and the web as medium-plus-join; the AI monolith; night as the same move carried to the technical level) — and called for the README to be rewritten by the one rule trusted most: the why before the how. Written, then corrected through three author rounds: honesty (nothing "evaporates" — the deficiency is one of *kind*: words, not structure), heroing (the typed medium as the lever — programs compound, the interface builds on primitives as data does, the agentic harness as a program in the medium), and voice (interconnection over building-on; the claim scoped to AI; the polarizing ending softened). Then the method experiment: four blind writers, briefed with a proposition inventory and no repo access, each writing the head independently. Finding — **the variance collapsed**: a tightly compressed proposition *is* prose, and all four reproduced the brief's sentences and order despite instruction; the compression level of the brief sets the freedom of the writer. No knowledge was lost in any variant (the inventory held); the yield was sentence-grade joints, harvested (the floor-for-the-next-one opening, the where-the-Unix-lineage-ends pivot, the deficiency-of-kind landing). Conventions' knowledge-structure section took the day's statement — as much as we know, stated not proven. View.md's ratification stands paused mid-file; earlier same sitting it yielded the strict-mount ruling, the no-commit-on-event restatement, the enum sweep, and design.md's move beside the specs.

**The ratification read opens (2026-08-23) — sdk.md first, and the read ops collapse.** The author set the priority: the end-of-file opens wait; what looks settled and isn't right goes first. On sdk.md the one note was discoherence with the law — `read(places)`, `resolve(term)`, `get(id)` are one act, and `ReadOpts` was a second query language beside the expression language the law calls the only one. The steward reasoned it through in words first (a question is not a work order): the triplet dissolves into `read(expr)`; the options are native verbs, most already existing, three new (`match`, `skip`, `survey`); `subscribe` and `read_batch` follow; `anchor` and `branch` stay outside as context, not query; db.md's `ReadOpts` drops to the plan interface where no author sees it. Two opens surfaced and, at the author's push for recommendations rather than pointers, were recommended and ruled [P]: *a name evaluates, a place fetches* — the engine tells an expression chunk by its placement, the same discriminant the match uses, so the wire needs no new tag; and *text, no helper* — ol grammar is TS expression grammar, the engine parses, and a helper is priced in `view/sdk` the day a component wants it. Folded whole in sdk.md (*Reads*, *Types*), the protocol table and planner list in engine.md, surgical lines in substrate/agent/db. The author's phrase for the tag: "the lighter type of settledness."

**The surface rewrite (2026-08-21/22) — the spec tree rewritten whole from the brief.** The full cycle in one long sitting: per file — outline where reshaping demanded it, whole-file recomposition (Write, never Edit), then two adversarial audits by fresh subagents (a loss audit tracing every old claim to carried/superseded/moved, a honor audit verifying every brief statement landed with its [R]/[P]/[O] tag), findings folded before commit, a fold report to the author instead of a reread. Landed in order: **engine.md** (the artefact split, the attach era, the call context as the boundary law's center, ceiling cap→source with `caller`, offers-bind in the match, daemons; ~180 claims traced, 0 silent mutations) · **substrate.md** (surgical: attach sweep, instance-removal ruling, ref-refinement open) · **chassis.md** (host.md reborn: hospitality, the entry, the input floor, the home; one cross-file consent contradiction reconciled [P]) · **view.md + components.md + desktop.md + bootstrap.md** (programs.md dissolved along the module structure after the author caught the compromise; ~250 claims audited, four vanished-settled things restored — folding-is-a-pipe, the headless catalog, sequence/document, the spine rule) · **sdk.md** (one transport object [P]; the ordered-selection contradiction resolved; anchors replace seat tokens) · **pilot.md** (the monorepo, the pilot cut, build order re-founded). Closing sweeps: db/agent/horizon/sketches vocabulary; horizon's engine-as-daemon entry marked landed-in-law; the engine outline retired. **Method lessons, paid for:** a blind rename sweep briefly rewrote history (log, research, board narratives) — restored; the law now stated in the session: renames retarget links in living files, history keeps its own names. Author checks ruled en route: C1–C4 (uses/presets dropped, collation to view, instance removal, exit retired), the ctx-gate relaxed (resolve by making), source serving trimmed to pilot grade, blank lines between list items (conventions). Same sitting, before the rewrite: **conventions.md rewritten** on the week's hjulverkstan learnings (records are events; outline-before-prose; name what you leave out; context craft; the knowledge-structure section refined and pointed at the ladder). Queued for a cheap session: the conclusions.md audit-and-close, the agent.md deep-claims sweep, fixtures. The brief's own retirement awaits the author's ratification read.


**One source of truth — ratification.md retired (2026-08-20, same sitting).** The steward's check found the brief citing ratification rows by number (3.3, 3.4, 5.3–5.6) and two open rows the brief never carried; the author ruled both and called the purge — "if that is the only gap then we purify to one source of truth… better that we fold in what is needed." **3.2 — daemons, ruled:** impure programs are as launchable as pure ones — the walls (`run`, capabilities) govern and purity is never the gate (it only decides what the planner may evaluate); nothing auto-starts — a pilot daemon is started by a person's act and a component whose service is absent draws the honest fault face (the author's docker-cli-without-engine posture, accepted for now); gestureless starts deferred with it. **7.1 — knowledge structure:** no principle yet — work by established prose practice; `conventions.md` gained a marked-open section holding the author's brain-picked ground (folds necessary, bottom-up sometimes required, dependency order crucial; comprehension grows bite-wise, ordered by knowledge dependency; in tension, the entry read must engage — goals before the folds that tell how). The six cited opens and 5.1/5.2's reader drafting material were restated inline in the brief; every other open row traced to a supersession or existing coverage before deletion; the board's and conclusions' links annotated. `ratification.md` deleted — git keeps it; `act-scenes.md` and `design-language.md` stay, grounding the brief still cites.

**The review turn (2026-08-20, same day) — the fold challenged and corrected in place.** The author was surprised by the fold turn — paragraph-stacked edits, nothing talked out — and asked for a review of it. The findings, owned: the steward had skipped the dialog it had itself named as needed (the call context's name and contract shape), minted the tiler's verb set undialogued, and recorded steward elaborations in `conclusions.md` under a "ruled" heading — the recorded failure mode ("minted without dialog"), repeated a third time. Substance defects the review caught: *"`engine/process` conforms by construction" was false* — the frame is a fifth reach source, exempt from the parent cap, which the drafted generic contract does not carry (now the named open seam, in the brief and its index); [R] overclaimed on the call-context shape (the direction is ruled, the shape and `anchor` name are [P]); the toml's name and placement were a guess. Corrections folded in place: established naming kept (`.ol/project.toml`, contents superseded — `[[attach]]` replaces `[[mounts]]`); retags throughout (call context, tiler); the tiler verbs' declarer marked open; the conclusions entry trimmed to a pointer at the brief. **Ruled en route: the session is minor state of the pilot desktop module** — not an engine concept; the engine's `launch` owner is configured; two duties earn the chunk (the writable indirection between a shipped read-only entry and the live arrangement; the work group that lets kill cascade and gives the sidebar a "work" to render). Opened by the author, not ruled: whether `conclusions.md` earns its place — for the surface arc it held nothing the brief doesn't; proposed audit-and-close rides the rewrite (board, the reconcile remainder).

**The feedback fold (2026-08-20) — five notes on the ratified brief; four folded, one held open.** The author read the brief at a glance (~25%, most paragraphs recognized from before) and left five raw notes (`feedback.md`, committed with this fold, then removed — git keeps it). Steward positions were laid out, the author ruled in one pass, and the brief was amended in place (header carries the amendment line). Folded: **sources are engine-served [R]** — the brief's own lean, ratified; files may become their own integration down the line. **The mount context became the call context** — the author caught `Context { mount }` as a view archetype leaking into the engine; resolved through the design's own symmetry: the engine already judges a process by its call (declaration + argument + grant + parent), so that shape became the engine's own structural contract — `Context` gains `{ anchor }`, effective reach intersects up the chain to the machine context, `engine/process` conforms by construction and **`view/mount` conforms from the view side**; the engine never names view (the dependency law holds at this seam too); the parent-link derivation is the open encoding. **The home is a standard store directory** — the settled recognition (a dir with `.ol/` inside, db and toml within) was the law all along and `field.ol` was the brief's deviation; corrected, and the consequence taken: `--home` may point at any store directory, a project included — the project-agent line ("a session sits in the store it was opened for") cashed with zero mechanism. **The simple tiler** — the author dropped tabs and sidebar from the pilot shell but held the tiler as the piece that cannot be hand-waved; specced simple: the shell template places `$session.root`, a tree of `split` and content (`tab` dies; `session` shrinks to `{ root }`); the tiler is verbs and one gesture — *split* as a command verb from the match wrapping the target in a new `split`, *close* as the ruled body edit with single-child collapse, *resize* as the split implementation's own pointer act committing `ratio`; move/swap and WYSIWYG ride the post-pilot drag layer, which the tiler doesn't need to be usable; sidebar remains a declared component, mountable, out of the shell. **Held open: the session's standing** — the author asked whether session exists at all now that it left the host; the brief holds it in four roles (`launch` ownership, input-record ownership, the shell's argument, the arrangement root); the steward lean — engine generalized to a configured owner, session surviving as desktop data if at all — was agreed as direction with the talk-over owed before the rewrite touches those sections. Marked [O] in the brief's desktop section and open index; the board's Next 0 gains it as a pre-step.

**Ratification and the check-in fold (2026-08-15/19, across several sittings) — the surface arc closes on one brief.** The author opened ratification with a chronological brain-pick of the first proposal; the steward grouped it into seven topics on a working page and closed topics 1–2 in dialog (capabilities `net`/`fs`/`exec` with modes, recorded-not-enforced before the VM — the keychain an OS citizen, not a capability; secrets a module; the engine its own installed artefact; `serves` for grades; settings as optional entries; no hidden state; collapsed/fault faces for "valve"; blocks as archetypes by field; native / program verb). Intents resisted ruling from a sketch, so fourteen stories were walked in `act-scenes.md` against two models. Then the author **opened a deliberate fold** — *is there something simpler than the whole proposal?* — and the fold ran the rest of the way: security and performance proved a wash between old and new (the wall is the realm in both; a thousand iframes is not viable in either); WYSIWYG was not a moat; the process → surface move stood on its own; the author pushed the stack runtime > renderer > medium > component down to **two techs — a surface (hosted + glued) and components** (self-contained executables met through adapters, Solid inside, binding not diffing); theme dissolved into family settings; steering became **three selections down the mount tree** (reach, `draw`, `offer`) after the author rejected a glue-owned ladder ("without the glue becoming proprietary"); and the decisive last turn — **argument as a selection, not kv — made the family the tree's own pair: component ↔ program, mount ↔ process** (`view/mount` as the view's process, slots as `ref(view/mount)` entries). A second spine-only read caught the remaining gaps — closed slots need a ref refinement (new grammar, open); `view/surface` split into kind and config; offers in code via the adapter; and content the steward had compressed away (the design language, boot and projects, the agent's placement) **restored in full** — after which the author found the real gap: modules, dependency, loading, what you run. Resolved: **a module is a store; dependency is attach; the attach record `{path, branch, at?, write}` is one shape everywhere** (toml, API, field — write declared by the person, enforced by the engine; write-on-a-branch the middle ground; "fork to edit a module" retracted — clone and attach writable); **the chassis declares its entry contract** (layers + reservations, the `--mount` shorthand replacing any safe mode); **the home** (`ol.toml` `[[attach]]` + `[chassis] entry`, `field.ol`); **the desktop module** ships the entry and **the shell as a template over base** (a tiler and a tab bar too rudimentary to be components — vanilla over abstraction), sidebar and projects as components; `attach`/`detach` as engine programs over `[engine/attached]`. Method notes, honest: the steward twice restored or minted without dialog (first-generation content into a changed field; `view/mount`-as-multi-typing; "fork" for modules) and was caught each time; the author read only the spine on the last pass, by design. The author called go without a final review, asking for confidence checks instead — term audits were run on every version, and a fresh verification agent checked the brief against the record. **End state: one file — `spec/research/surface/proposal.md` — the brief; `ratification.md` closed as the record; `act-scenes.md` marked resolved; both prior generations deleted (git keeps them); the author's raw notes (`feedback.md`) kept for this commit because they were never tracked — the verification agent caught the false 'git keeps it' — removable after; the ruling recorded in `conclusions.md`; the board's Next 0 is the rewrite of the spec tree from the brief, with `ctx` and the adapters to prototype before the view family folds.** Side result: the *deliberate fold* gesture recorded as a marked direction in `agent.md`.

**The surface arc (2026-08-13/15, one long sitting) — the review matrix ruled through, then the interface model re-derived whole; proposal awaiting ratification.** The sitting opened on the review matrix's findings and ruled the authority cluster in dialog: **the boundary formula** ((frame ∪ argument-grant ∪ ceiling ∪ additions) ∩ parent-cap); **absent ceiling key ≡ `{}`** with openness as the explicit **`caller`** element (the author talked the steward out of absent-inherits: "it just becomes explicit" — composable exclusions fell out free); the capability lattice (net/fs/exec/keychain ∩ parent-to-child; a minted program's claims are requests); instance removal requires write; **secrets as an integration** (value-less stand-ins; `read-secret` the sole value path, walled by `run`; native-runtime for the macOS keychain); `resolve` under the run wall; **kill-by-ownership / close-is-layout / unmount ≠ death**; escalation's consent surface (host modal from field reads + the reserved native chord as the binding act); root-from-config with pinned boot expressions; sync-then-evaluate for remotes. Then the author opened the fold that consumed the sitting: **why not drop React entirely — the interface as data over one renderer?** The dialog ran through projections, islands, and person-as-principal to the settled model: **surfaces are not processes** (processes stay work; the interface's principal is the person; interface commits attribute to the person); one realm, JSX + signals (**Solid**, React not chosen); no durable in-memory state (**ephemera** with lifetimes; the engine's `buffer` untouched); components as contract + per-renderer implementations; **medium** (block-archetype families by ownership) / arrangement (db-validated typed bodies, stored or derived) / **mount** (`{of, surface, prefs}` — **surface always stated**, the author killing the steward's render-time inference: dynamic choice lives in **parent-owned defaults tables**, recorded on the mount at composition); **surface** = `{argument, root}` (medium derived by walking the root; collations = **ordered mounts**, the member layer resolved away); sizes-as-the-only-contract (**box**, name free) with the **valve** (compact face below range; depth becomes navigation); the **trusted-input-record contract** (chassis reservation table captures privileged input natively, delivers what+where; the overlay becomes just a module — the chassis stops knowing overlays exist); **theme as data** (token values + implementation selection — not a codebase); **attach era** (stores ATTACHed, one evaluator over the union — deliberately reversing "federation in Rust, not SQL" as the review matrix's proven leak; commits per-store by definition; cross-store gestures as safe-prefix sequences with citations carrying unity; the id debt goes blocking); **personal-field boot** (`~/.config/<host>/`, projects as attach acts, project-vs-personal agents by where the session sits). Method note, honest: the steward drifted from the dialog three times (library/install ceremony, optional mount surfaces, the member layer) and the author caught each on read — the corrections are folded and attributed. The document went through three generations (linear narrative → same with vetting pipeline: disposition + three blind agents + cold reads → **module-graph form** at the author's direction) and lives at **[`spec/research/surface/proposal.md`](spec/research/surface/proposal.md)** — flat kind-prefixed monorepo tree as the opening, one section per module in dependency order, chunk definitions per archetype, opens per module; `arc/surface-proposal.md` stands superseded. The author began the ratification read, noting **cold feet at the scope** — the steward's accounting: most rulings deleted machinery, the futures were scouted not committed, the pilot cut is small (one store, one chassis, one renderer, one medium, three surfaces), and the three real risks are named (surfaces-not-processes vs built code; the unexplored newspaper grammar; the expressiveness bet — proven or broken by the flat reader, the kanban drag, and chunk-table). **Owed: the ratification read's completion, then the record into conclusions.md, board/Next rework, the mechanical sweeps, and the pilot-cut page.**

**The small-rulings cluster (2026-08-12, same sitting continued) — every batch-raised gate closed.** The steward split the cluster by the recorded convention (technical layers steward-owned) and brought seven concept-level rulings; dialog reshaped two of them into better designs than the leans. **The tile tree**: the author resisted `owned`-as-structure ("namespacing might not be equivalent to tile structure... we can also utilise field relations, they are very explicit") and was right by the grain rule — the tree became **typed fields** (`session.tabs`, `tab.root`, `tile.children: list<ref(tile | engine/process), 2>`), which dissolved three problems at once: `seq: true` retires from the arrangement, the naming rule never fires on tiles, and flat ownership under the session gives the writers a one-hop `[session]` boundary. Union ref-constraints entered the vocabulary; the doctrine sentence landed (ownership is the implicit residence; deliberate structure is authored as fields). The leaf question ran deep — bare refs, stateful slots, reader-owned choices — and settled: **the leaf is the process; slots are never stateful** (state lives in readings; re-seat via menu over the same reading), and nested-slot preferences are reader-owned at two grains — the per-occurrence exception recorded as **the pinned surface call** (the seat made explicit; no consultation ladder — author correction), shape-rules as collation settings; the context menu **composes by layer** (surface · seat · reader · registry — into the queued menu unit). **Native vs single-request** unknotted on the author's push ("some native programs are not single-request, right?"): `runtime` is only who-executes; whether a verb lowers is the planner's own knowledge, derived like purity, never typed. The rest ruled to lean: first session owned by the active project's root (project-based now, unbounded future noted) · frame reads `[self] | follow(owned)` · `links_modified` event-only ("truth and indexes for performance are different things" — revisit with worlds/object-model) · existence-oracle leak accepted for v0.1 · archetype hop as derived admission · **temporal queries into v0.1 scope** (author: "what is wrong with shipping an `at` program?" — nothing; pilot's defer deleted). Steward calls unobjected: governance at db, one `$type` encoding, projections declare their own ordering, `read_batch` identity tokens, host-direct citizen return path. Folded across engine, programs, substrate, db, sdk, host, bootstrap, pilot; Gates rolled up. **The follow note then landed in the same sitting** — engine.md gained *Hops and `follow`*: six hop verbs (`members`, `placed`, `owner`, `refs`, `backrefs`) plus `prop`, follow as closure-of-a-step (any pure chain; depth; visited-set termination; deterministic yield order), closure output carrying its walked edges (the branch/join renderer's requirement; wire shape at the SDK build); the planner's read-native list, both boundary-grammar sentences, programs.md's native row, db.md's Closure notes and the board's namings all swept consistent. **The seat model then closed after a misstep**: the steward first proposed a four-program split, then — misreading the author — collapsed chrome into one program and folded without alignment; the author called it ("you took liberty to write without alignment"), the fold was reverted from git, and dialog resumed. The settled shape, the author's: the root host program is a program like any other; sidebar and tab-bar are **also** programs, filling the shell's chrome seats — and those seats are **pinned** (`sidebar([session])`, `tab-bar([session])`: deliberate seats, no match, no swap; the pin doing chrome's job). The host boots the shell alone; pinned seats start their programs as the shell's children; the palette stays host-started and host-placed (it holds no session write); accepts are bare session refs, killing the `request` payload archetypes; `collation` seeded under engine, `reading` under the reader. Root-as-pure-tiler with chrome as choices recorded as the later, more modular direction — the pilot stays simple. bootstrap.md's last marker cleared. **The interface sitting followed (same day, long dialog; provisional by the author's own words — "acceptable for now, learnings down the road", closed under spec fatigue).** The path: the author asked how overlays actually work; the base-page question ("is there a host-owned webview?") sharpened into the ruling that **the base page is host code** — root slot + overlay layer, renders-never-decides, the root program seated and swappable; overlays became all-fields (`{anchor, content, grade?}`, no placements — ownership is membership) with **grade chosen at dispatch** since no prior box exists; the gesture split (secondary = seat machinery, ordinary = the surface's); menus grounded in [`menu-scenes.md`](spec/research/arc/menu-scenes.md) (nine scenes) and refined in dialog to **entry grain = payload archetype** (one arranger, many verbs — the author's "a program per command is unfortunate" catch), **`actions` declared as data at two scopes** (render chain = view verbs; offer's types = thing verbs — the author's "not substrate native?" catch killed the registration hook), **one command program at two grades** (the author's unification: the menu with an expand, the palette with a browsable offer; name open), `board` renamed **`arranger`**, **starting and landing as two acts** (`arranger/open`, `position` as value chunks; the run button = those calls pinned as a surface-authored split button — the author's "no magical chrome" correction), and the **context-menu/value-select line**. En route the steward misread twice and was corrected twice; the scenes-map method carried the ground. **Remaining before impl: fixtures, the ratification read (now including this sitting's folds).**

**The model-seam review (2026-08-12) — the author's first read lands corrections; the request dissolves entirely.** Reviewing the E diffs, the author pressed on two wordings and one structure. The wordings: *never widen* is false as an absolute — a run may exceed its walls, it just requires approval — so engine.md and programs.md now say **no run widens its own walls mid-flight; widening is always a new consented start**; and `run` staying `selection` (not `set<ref(program)>`) was confirmed for expression walls (`[engine/program] | where(runtime: native)`). The structure: the provider-shaped request body meant **the agent was not decoupled and `model` was not runnable from the substrate** — the author rejected it, and the dialog that followed dissolved the request in stages, each author-driven. First: assembled bodies replaced by **the offer** — `model` takes a selection; the offer is the grant (boundary source 2 doing its intended job); serialization is deterministic provider code; **the argument becomes the consumption record** and `includes` dies unbuilt. Then: the output must be **canonical and shared** (`model/output` — content, thinking, residue, ordered `calls`, usage) or coupling re-enters outbound — author catch. Then: **tool calls are drafts composed by the model program itself** — the author's gut, initially argued down by the steward, then conceded: a draft is inert data, the seal (`run: {}`) holds because the act is the start; the agent starts within-walls drafts and beyond-walls drafts simply rest — run-to-draft becomes a resting state, not a path. Correlation collapsed twice: first to a call-id convention, then — author simplification — to **`calls: list<ref>` on the output; wire identity is field identity** (replay tool_use ids are draft chunk ids). **Derivable-over-verbatim blessed as first-class** ("if you cannot trust the substrate, the compute environment breaks"): wire request = f(argument, `at`, versioned provider program). Last: the window is ordered but a selection was a set — resolved by making **`selection` ordered** (`list<loc | ref | expr>`, duplicates rejected as a stated clause; order is the consumer's to read; supersedes ordering-as-serialization — order moves to composition, visible in the seated argument, cache stability the composer's lever). The worked walk that grounded all this surfaced one gap, left as a **marked steward lean**: a caller must read its children's results (two hops), so the frame's read term wants to be `[self] | follow(owned)`. Folds landed same-day: agent.md (model, record, cycle with the worked turn, escalation, seeds), substrate.md (`selection`), engine.md (shapes, match wording, walls order-blind, the frame lean), bootstrap.md (the `model` dimension; sealed tools gain `run: {}`), programs.md (§8 result naming, §9 on argument links), selection.md supersession marker, conclusions §E addendum. The author closed the scope: *"we now have settled a good foundation — I do not demand more out of agents for this time."*

**The agent sitting (2026-08-08/11) — E ruled end to end; agent.md rewritten; the worklist closes.** The steward's position paper ([`spec/research/arc/agent-position.md`](spec/research/arc/agent-position.md)) re-derived the agent whole under the rewritten law, on one stance: the agent should need no mechanism of its own. Four dialog rounds then ruled it. **Context = the argument** — `accepts: [selection]`; the context-list/context-chunk structure dies; staging is place algebra on the draft; *talk about this* is a draft citing the thing. **The record is intent and fact** — the frozen argument expression on the turn; per-cycle request chunks carrying `at` and `includes: set<ref>`, so consumption tagging is a `linked` answer and ordering is serialization policy, never structure. **`run` landed as the third boundary key** (author question — "is that a third field, read/write/run?" — answered yes and folded): the toolset *is* the run boundary, `model`/`web`/`filesystem` became enforced leaves (`run: {}`), and the cap composes. **Run-to-draft escalation** replaced gates entirely: exceeding any wall composes a draft child, auto-surfaced; approve is starting it with the approver's reach as cap — approval is lending authority, the only way reach ever widens; deny is `cancel` → `failed: denied`; a question with no action is discourse, and `gate` died. **Controls** settled as chunks with the context-menu as their UX (pause + prompt aimed at the work), purity made checkable as an enumerated delta list. **Assembly**: include-everything-normalized (dedup is lossless; the union ≈ shared context once + per-turn prompt and answer), no silent reduction — grades only where specified or the agent's guidelines direct; self-serve reads default. **Sub-agent citizenship is flat** — ownership means lifecycle and cap, nothing else. En route: follow reframed as closure-of-a-step over connections (the hop vocabulary is the real missing piece; `prop` promoted to load-bearing; the edge-carrying output shape logged as a defect); the context-menu/palette equivalence pinned (one family, two anchors); composition UX charted scene-by-scene in [`composition-scenes.md`](spec/research/arc/composition-scenes.md) (fork/merge as inline sequence elements with branch TLDR slots, diamonds continue past their join; the sidebar possibly itself a reader; the argument→window overview as the agent's shipped citizen surface); a day's containment/storage rabbit-hole distilled to [`object-model.md`](spec/research/arc/object-model.md) (content addressing for remotes — the rest did not survive its own scrutiny). Then the fold, author-directed rather than batched: **agent.md rewritten whole** in dependency order (three pending opens closed in the writing — deny = cancel of a draft, policy = the three keys uniformly, question gates = no mechanism; four deliberate opens remain, each naming its settling condition); engine.md folded (`run` key through contract, process body, boundary sources, RunArgs; run-to-draft under Lifecycle; cancel-of-draft as deny); bootstrap's agent seeds written (model programs + `agent` + `prompt`/`settings`/`answer`/`control`; no session, no conversation, no gate, no context archetype); worklist closed. **Owed: the author's ratification read (now including agent.md), the small-rulings cluster (tile containment blocks host boot), the follow note, fixtures.**

**The fold session (2026-08-07/08) — the worklist ruled out, the batch executed, the law rewritten.** The sittings first: **B** retired *scope* wholesale (the op is `read`, the vantage a place, `dispatch`→`start`, placement `on`/`kind`, the chunk field flat as `instance`, *the match* vs *the gate*); **F** dissolved session.md into agent.md and slimmed pilot.md; **D** ruled no deny, a dimension needs a name, and instance contracts stay open — closure would force "closed over the union of every archetype's keys" for a win the type-mirror already pays. `seq: true` settled after three wrong turns: dissolving it lost the `list`/`set` distinction, and emergent orderedness could not bootstrap a fresh place; it lands as a flat top-level field legal only on archetypes, auto-assigned `max+1`. **Then D1 cracked open.** Asking whether a chunk may deny access below itself surfaced that the permission layer was underspecified rather than missing a feature: a read answers across all five connection kinds while reach walked ownership alone, so anything `relates`-placed on a granted chunk came back with its body — relate something and you publish it, unintentionally. `boundaries.md` (retired; in git) diagnosed it; the author widened the frame instead of choosing inside the ownership model, and **[`dimensions.md`](spec/research/arc/dimensions.md)** is the result — every chunk a dimension, placement is publishing, ownership demoted to naming with no transitive walk, boundaries as selections, filtering uniform, counts describing what your boundary admits. Eight steward findings folded back into it; finding 1 restructured the model and dissolved three others by cascade. Two rulings taken in review: membership is always current including under `at` (securing a chunk hides it throughout history — what remediation requires), and prose reads openly while surfaces cannot run over chunks beyond reach, so the ladder needs a beyond-reach face. **The batch then ran on trust**, six files by subagents from [`conclusions.md`](spec/research/arc/conclusions.md): the tree moves, the vocabulary sweep, substrate rewritten on the dimension law, engine rebuilt on it, host slimmed to Rust authority with the arrangement layer moving to programs, db making it physical, sdk putting it on the wire. `bootstrap.md` and `agent.md` were deliberately left partial — the tile-tree containment kind is open and the agent model awaits E — with their stale claims named in place rather than left to be found. **Method mattered as much as content.** The first substrate pass inserted the new law beside the old skeleton and patched thirty sentences to agree; the diff gave it away (38 hunks, all but two single-line). Redone with `Edit` forbidden and a claim audit required, it recomposed in dependency order — kinds before membership, contracts before `selection` is used to define a boundary. The audits earned their keep repeatedly: four claims restored in engine, two caught falling in the gap between host and programs. And **the defects that mattered were found by the passes forced to *use* the law, not read it** — the `selection`-per-contract rule over-generalised from `accepts` entries, `SelectionTerm` missing its `Ref` variant, `selection`'s purity clause pointing at a definition that did not exist, `links_modified` not projectable because links are outside history by law, the free archetype hop law-grade in substrate and absent from every boundary source. The author's **`ref`-in-selections ruling** came the same way: X ≠ [X], so without it offering a bare chunk needed an expression subtracting a place's members from itself. Also this session: research triaged (eleven records retired, the live arc gathered into `spec/research/arc/`, seven decoupled surveys kept), and three AI-compute notes recorded — rewrite-versus-edit with its audit cycle, turns as substrate (ingestion stops being sunk cost), and compounding reflection (revelation as a function of chains, and the risk that it compounds error as readily). **Owed: the author's ratification read, then E.**

**The worklist session (2026-08-06) — the author's review structured into one queue.** Three commits stood: the staged redistribution, the author's margin notes (37 inline blockquotes across substrate/engine/programs/sdk/agent, written before the selection arc), and the parallel session's two dialog records (one-compositor.md, selection.md — supersede the staged argument/boundary model on paper). The notes distilled into **[`spec/research/arc/worklist.md`](spec/research/arc/worklist.md)** — the single tracking home, only what needs attention, one item at a time by author instruction (raw notes stay in git, d74d932) — and the specs cleaned (three stray typos from note-taking reverted with them). The notes sorted honestly against the later arc: seven already answered by it (demand→flat read/write, the form's dissolution, block notation, planner/SQL, `at`'s home, locks-as-exploration, the kv sentence), the vocabulary cluster gathered into one sitting (scope/location, read, run-vs-dispatch, invocation, scope_id, spec/instance, the gate collision), six substrate shorts (protected sub-roots, name-for-identity, open-vs-closed instance specs among them), and the agent arc gathered under the author's explicit instruction — the steward owes a re-derived position paper on the whole plan before that dialog. Two margin sketches (type-mirror integration, web-projection integration) landed in sketches.md with grounding. Tree decisions queued: session.md's existence (the author, twice: sessions were dissolved; the filename asserts what the law denies), pilot.md's multiple-sources-of-truth. Board now points at the worklist as the working queue; the engine's technical layers recorded as steward-owned review territory (author skips them deliberately).

**The redistribution session (2026-08-03, fresh session on settled.md's mandate) — the specs aligned to the settlement, from scratch where the disposition demanded it.** The steward bootstrapped, read the whole tree (all nine target specs + settled.md + typed-bodies.md), grep-verified the stale-map, and surfaced the settlement's silences to the author before writing. Author rulings taken in session: **run boundaries live as typed process-body keys** (`read`/`write` list<ref>; boundary chunks and their relates topology retired — the placement-as-reference pattern typed bodies cured everywhere else); **all five connection kinds surface in scope reads** (owned/instance/relates as membership with three counts; field/mention in the separate `linked` answer); **status is `draft | running | done | failed`** (pending collapsed to engine-internal; completed→done a code rename); engine.md rewritten sectionally (code-verified machinery kept, superseded sections rewritten whole). Then the mandate executed: **substrate.md from scratch** (instance spec as law, five kinds, links derived at write with permissions both ends, reach = ownership + grants, worked example, opens consolidated); **engine.md folded** (program body as interface, process record every-key-typed, one-placement-check validation — the accepts machinery retired on paper, trace exemption dissolved with it; draft/run/launch lifecycle; reach constructed not filtered, with `demand` absent-vs-empty distinguishing open programs from frame-only ones; a new Expressions section carrying §3 whole; `await` returns the process; startup reconciliation spares drafts); **db.md physical** (`owned` in the CHECK, `current_refs` + indexes + refile step, `linked`/`Link` shapes, instance-spec validation kinds, name-unique-per-owner, `links_modified` on Commit, expression-index promotion); **sdk.md** (boundary translation native ⇄ tagged ⇄ stored, frozen-vs-head resolution modes, full type sync, new RunArgs); **programs.md from scratch** (actual programs only — frame/sidebar/palette §1, form §2, reader §3 with the spine rule, prose §4, verbs/citizens/slots §5, process-view §6, history/review/merge/arrangement §7, catalog + result names §8; mechanics referenced never restated; R-number scaffolding dropped); **bootstrap.md rewritten** (instance specs, status value chunks, interface-archetypes-owned-by-their-program convention, no containers seeded); agent/session/host/pilot swept (ol: scheme, member/pairing, instance-spec composition types, ownership-chain paths, section retargets). One steward reading taken and **marked open in engine.md: the frame is the process's ownership subtree** (children/results owned by the process, launch/drafts owned by the session) — the reading that keeps "a program reads its own frame" true under reach-via-ownership; flagged for the author's ratification read. Board resynced: pause lifted, spec-leads-code-follows divergence pinned as the alignment pass, settled.md stamped landed. **Owed next: the author's ratification read, then the build queue (reader v0 →) with the code-alignment pass woven in.**

**The settlement session (2026-08-03, author dialog end to end) — typed bodies through the settled model, committed as cfb2533.** The arc: typed bodies adopted (write-in-body, graph-grows-the-shadow; knowledge modeling in, ownership the fence; permissions both ends; storage unchanged — one JSON column, links as one derived table); the instance spec named and collapsed (spec = typed key-map for instances, never for the chunk itself; towers natural; per-key `?`/`unique`; accepts/required/propagate/body.schema retired); the connection taxonomy completed (owned-by · instance · relates · field · mention; reach = ownership + grants); enums as value chunks; the call frame floored (interface in the program body, argument chunk instance-on-archetype only, process body argument/at/status/result, one-placement-check validation, frozen-vs-head SDK modes, accepts retired); expressions settled definitively (locations, named-arg calls, grouped expressions with out, collation as members+settings+predecessor, names-vs-refs by grain, classical written language — no pipes, no positional args, braces for literal structure, trivially parseable; spine rule for small UI); the reader model corrected in review (member not binding; cards and history dissolved; slot chrome not marks; document dissolution flagged). All distilled into **spec/research/arc/settled.md — the single authority** (threads-dialog.md demoted to path); redistribution mandated: substrate takes the type system as law, engine takes the call frame and expressions whole, programs.md rewritten from scratch as actual-programs-only. A cold-agent dry run validated the handoff (model restated correctly, plan derived correctly) and surfaced twelve hygiene defects — board namings/gates staleness, missing log entry (this one), pilot.md links, README reading order — fixed in the follow-up commit.

**Correction stretch — five fixes, two dissolutions, two opens (author read-pass, final).** Tab double-use fixed (the word left §3.5; the host owns tabs). Reading/collation swapped to the author's mapping: collation = the immutable composed whole citing its predecessor (the lineage is of collations); reading = the persistent store whose relates points at the current one. `history` dropped as a default binding — db/commits is just `sequence` + the ladder + slot chrome. Marks dissolved into **slot chrome** (per-location color-dots + connection counts; scope-in-place expands connections inside the slot). Folding became a pipe (`fold(summaries)` replaces summarized elements; fold state lives in the binding's expression). **Templates/presets dissolved entirely**: collations are values — opening any collation is a fresh reading pointing at it, first edit branches, nothing copied; agents ship shaped by relating collations. Two opens recorded at the author's flag: the predecessor-citation carrier (relates is untyped — connection-chunk candidate vs typed-JSON ref) and agent contexts as selections (N-source contexts, piped contexts incl. self-written purifying summaries — unexplored). Specs and board swept; superseded words now: lens, focus, trail, eye, position, preset, template, marks, contribution.

**Naming settled, the binding model drawn, specs resynced (author read-pass, continued into 2026-08-03).** The author rejected braid (complecting — over itself again and again, not the phenomenon), the film register (spoken language, not professional), and mixed families; asked what a thesis-writer would name these. Answer found in textual criticism — the discipline whose whole business is deriving structure from citation (collation, readings, loci, stemma, exemplars, sigla) — then folk-simplified by the author into the settled family: **reader · reading · collation · location · binding · follow · template · marks**, with reader→reading→collation the author-locked spine. Trails, the eye, and the pointer all superseded: **readings cite their predecessors** (a citation DAG — no stored sequence, no deleted futures); the reader chunk's relates carries "current" (relates over body id pending typed JSON, now a horizon entry at author request). The **binding model** made the author's tabs description precise: every tab is a binding (a surface program + an assignment of collation locations, through pipes, to its argument roles); locations repeat freely across bindings; an unbound location gets a shape-matched auto-binding — there is always a surface; **the reader has no magic** (draft + answer "with chrome" is purely one `sequence` binding taking two arguments); pipes render visibly and are edited form-grade; **marks derive from the whole collation** regardless of which binding renders — the surface computes none of it; attributes are binding config, sequence-only in v0.1. Postponed explicitly: pipe-editor visuals, mark-roles, attributes beyond sequence, separable collations, adjacency detail. §3.5 rewritten whole in the vocabulary; §3.6, §3.7, session.md, agent.md, substrate.md, horizon.md, host.md swept consistent; board renamed throughout.

**Spec folds landed — the dialog written into contract (co-authored session, 2026-08-02).** programs.md: the four contracts drawn — `lens` (§3.5: focus as immutable argument reference; N peer positions as union-of-intersections with two-level chip grammar; history as a sequence of focuses with pointer — focuses as values; presets as template-semantics committed focuses; attribute slots as per-element joins; fold as lens-owned arrangement; contributions), `process-view` (§3.7: thin regions argument · frame · result, filling derived — form on draft, live frame running, result = frame ∩ declared archetype; editability = unconsumed; stale-display as general default), `form` (§3.4: one generated argument editor for palette, drafts, re-runs), `prose` (§3.5: markdown with slots, mention grades link/inline/block); §1 gains arguments-immutable/living-state-referenced and the result-archetype rule; §2 gains the draft state; §3.6 rewritten to the dissolution (thread derived from citations via `lineage`; conversation = named position; composer = form on draft; face-follows-context); §4 table renamed (read→lens, inspect→process-view, launch→form; dispatcher absorbed; `lineage` + `prose` added) with the result-type naming pass (plain `output` names owned by relation — slash paths are instance chains, interface chunks relate, author correction folded; pipes return substrate-shaped). agent.md: context recorded as expression (intent) + resolution (fact) at one snapshot; discourse→thread throughout; answer-home transmuted to draft-summons-form; pause discussion becomes talk-about-this (the recorded agent.md/session.md contradiction resolved by dissolution). session.md rewritten whole as the thread experience (title marks the supersession; L0/L1/L2 thinking layers, derived status, conventions survive; v0.1 order aligned to the board queue). substrate.md: §Grain (identity→type, state→body key; body as compressed field structure) + Views open updated to focus/preset. engine.md: `explode` on What Is Open (pipe output is substrate-shaped). host.md program list updated. Author read-pass began and corrections folded live: result names are relation-owned, never path-shaped (slashes are instance chains — author catch); the lens reworked lean after author feedback — thin chrome over set-focus/set-surface, `trail` named and shaped (ordered focuses + pointer, instance on the trail archetype only, display name derived from current focus like tab titles), the surfaces decomposed into separate matched programs (sequence/table/document/cards, syncing through the field), peer inversion settled by the decomposition, stale transcript-styling language struck. Read continues.

**Closing stretch, same session — four rulings landed, repo tidied.** Indirection adopted for lens state: a process argument is an immutable *reference* to a chunk holding the whole view definition (scope expressions — the "DSL" — plus view settings); edits are commits on that chunk; the stable reference makes the program restartable whenever (author's addendum); the chunk's name stays open, candidate `focus`. The dissolution transmutations adopted — author: "that is the whole point" — answer-home → draft-summons-form, the conversation container dies (a conversation is a named position, materialized only when named/shared/bound/peopled); session.md's ratification read is now a confirmation pass over threads-dialog.md. Drafts ruled substrate-resident — "there is no in-memory markdown; substrate it is" — persistence a consequence, not a policy. read-tile → lens rename confirmed. Repo restructure per author's direction: spec files → `spec/` (pilot.md included), the first-pilot TS prototype and the whole `archive/` (pre-pilot browser/openlight explorations, shell.md) deleted outright — git history keeps them; every reference rewritten (markdown links and code doc-comments, verified clean); the board split — `board.md` is now the lean working board (Now / Namings / Next / Ruled / Gates / Tracked debt / Notes), the session narratives moved here to `log.md`; `spec/research/threads-plan.md` folded into the board and removed.


**Thread dialog, third session — rulings landed, dissolution proposed, plan drawn (`spec/research/threads-dialog.md` the reasoning record; the plan since folded into [`board.md`](board.md)).** Author-accepted: **thread/conversation named apart** (thread = derived context-lineage from recorded provenance, computed never stored — git one level up: turns as commits, citations as parents, names as refs; conversation = chosen discourse view); **structural matching against specs** (nominal names as conveniences over spec-shape truth; never body-sniffing); **the grain principle** (does the property change while the chunk remains itself? state → body key, identity → type; a JSON body is compressed field structure, explode/implode the isomorphism — resolves threads-lens.md §3's tension); **face-follows-context** as the conversation's resting default (reading is free, including is a gesture, every element wears derived inclusion state); **cascade lives in the lens** (context is never transitive — per-turn compression; the surface walks). Breakthroughs, recorded as proposals: **no container entities** — sequences derive from process arguments citing processes; a **draft** process citing the last answer joins the lineage, so the composer sits at the thread's end because it *is* there; **lenses hold N peer positions** merged into one sequence (the scope editor is a DNF editor — the built retarget grammar is its intersection level; hide/show suspends a union term); **one thin `process-view`** over argument · frame · result, faces derived (editable iff unconsumed — the retarget and the frozen dispatch explained by one fact; result slot = frame ∩ declared result archetype, the queued result-type naming pass now load-bearing); **answers as sequences** with markdown-with-slots (round thirteen's `[[mention]]` syntax the seed; fractal with the thread); **attribute slots** as per-element joins (session.md's mutation strip re-derived as an instance); **presets are recipes** whose program is a surface — agents ship shaped with their own views; lens durable state *is* its argument (preset-carriable → argument; dies-with-window → ephemeral). **Transmutations pending author word** (the session.md ratification read becomes a confirmation pass over threads-dialog.md): answer-home → draft-summons-form; conversation archetype → position-that-earned-identity (naming/sharing/binding/participants materialize the position chunk). Namings pinned for code: `lens` (read-tile's deepening target, rename pending), `process-view` (not "inspector"), `form` (one program for draft + palette), `prose`, `position`, `preset`, `draft`, `attribute`, `lineage`, `merge`, `fold`, `contribution`. Next unit: spec folds (programs.md four contracts, result-type naming pass, agent.md draft + context-as-expression) then lens v0. Deferred opens recorded on the board: content-type register (markdown tag), well-known key vocabulary, abandoned-draft policy (default: rest visibly), cross-tile staging grant. Separately queued, untouched tonight: review of the build session's telemetry/warming code (author distrusts it; kept out of this dialog deliberately).

**Parallel track — sessions as graph + field expressions (author dialog, exploratory; no rulings folded).** Session multidimensionality via the GitHub-issues pattern: cross-references are dual-placed chunks visible in both timelines — a fork's seq-1 entity is the origin turn dual-placed ("branched from here" / "branched to X"); *talk about this* backlinks onto the source timeline; `[[mention]]`s could too. The call tree stays a hierarchical trace; the discourse space is placements — a graph, timelines as queries over shared entities. Views are compositions, not mechanisms: **scope → pure transforms → renderer** (dashboard, adjacency rail, mission-control, sessions-of-relevance are all this one shape; switching standpoint = re-grounding the tile; no standpoint entity — rejected as machinery). Emerging written syntax, **field expressions**: scope algebra (∩, relates-of-type, exclude) as nouns, piped pure programs as verbs (`prop(key)`, `where`, `average`) — palette-typeable, type-checked by the interface contracts (§1's pipeline consumer), saved-as-recipe when recipes land. Substrate stays lean: placements for membership, body for content, pipes for questions — no enumerator chunks for body values (status stays a key; considered, rejected as index-thinking). `explode` — key-value explosion of a JSON body into virtual chunks with their own instance placements — manifests dimensions on demand: **pipe output is substrate-shaped**, so the algebra composes over results (touches engine.md's virtual-scope open); a hot query materializes by committing the same output, promotion when proven, never upfront.

**Build track opened — instrument-first progression, guardrails set.** Ruling from the parallel-effort session: implementation starts on the settled mechanism layer while board *Next* 1–2 continue as spec work — the program layer's remaining opens say "settles by building" in their own margins. Progression (instrument before organ; approvals must be final where given): (1) fixture suite + db crate together — fixtures are db.md's failing tests, shared Rust/TS ground truth; (2) engine + SDK on the same fixtures, plus the in-process fixture transport; (3) hollow host — tao/wry rim over a fixture stub speaking the exact specced protocol; surfaces authored and reviewed *in place* (wry-truth, not browser-truth), agent lane stays in-process tree assertions; (4) geometry walk + layout-as-data (manipulation grammar is spec-gated open in host.md); (5) db/engine grow underneath, then the swap — stub out, engine in, surfaces unchanged (the seam's proof); (6) fiber connector at rim integration — live-tree agent access, four verbs, later the first integration driver; (7) claude program, self-hosting threshold. Guardrails: TDD per conventions.md (no test writable from spec → no code, spec is what needs work); only settled-spec surfaces built (conversation tile enters as the commissioned experiment, not before); fixtures are the anti-divergence seam; commits still require the author's word. State: **fixtures landed** (44 cases, `db/fixtures/`, harness mutation-probed) and **db crate implemented, all green** (full db.md layout, verified); **geometry walk landed, green** (16 tests, `host/src/geometry.rs`; direction→axis pinned in host.md); **hollow host landed, green** (62 tests, rim + specced IPC dispatch + FixtureStub behind swap trait; `cargo run -p host` shows three live tiles). **Engine crate landed, all green** (successor Fable builder; ~6k lines): 10/10 protocol ops, federated pre-validation (union-accepts composed cross-mount), boundary walk fail-closed, R5 read-only enforcement, R7 both ways, 28 SDK-reusable fixture cases (engine/fixtures/) + 23 Rust tests, reactivity lost-event bug found and pinned by test. **Ruling by spec precedence (steward, vetoable)**: db's placement residency check contradicts substrate.md §Peers — db drops it, engine's anchor-row bridge retires. Queued with it: bootstrap adopts the D6 pattern for R7 (relates-place engine/process on programs); smaller engine.md gaps recorded in the builder's report (relates-not-openable, get-null-before-boundary order, virtual-scope grants, host-ctx subscribe, Lagged swallowing, inline-args-vs-accepts → the `attach` open is load-bearing). **Host async reshape landed, committed** (71 tests; seam mirrors engine's sync/async split; wire pins checked against engine parser; swap checklist in the rim: Engine::open needs runtime context, HostCmd forwarding task, Mount/UnmountWebview variants, async shutdown). **In flight at usage ceiling — if dead, resume from disk under these mandates**: (a) TS SDK at engine/sdk/ — sdk.md is authority, engine/src/protocol.rs is wire ground truth, three transports (wry/stdio/fixture), consume db+engine fixture cases via adapter, bun test; (b) db ruled-deltas at db/ — unresolved-roots metadata on ScopeResult, seq-tie fixture, drop placement residency per substrate.md §Peers precedence, all suites stay green. All landed and committed (specs → host → db → engine → sdk; workspace assembled, rustc 1.97). **In flight: the swap unit** — bootstrap seeding (per bootstrap.md; .ol/db for host + engine projects, minimal active project passing boot validation) + name-lookup convention (resolve canonical chunks by name within scope; readable ids stay tracked debt) + the swap itself (FixtureStub → engine::Engine behind EngineApi; boot steps: runtime context for Engine::open, mounts cascade, HostCmd forwarding task, mount/unmount webviews, async shutdown). Then: one surface program (read-tile) for author review — spec-readiness audit verdict READY-WITH-GAPS; five unmarked holes steward-pinned (fold into programs.md §3.5 next pass): inference table checked top-to-bottom first-match-wins; unresolved roots render as explicit dead-reference state (the ruled metadata reflected into the tile contract); name falls back to truncated id; only Document mode distinguishes placement type (v0.1 default, open for revisit); read's referenced-chunk errors render inline quietly (edit's precedent). Builder aligned mid-flight, including the tree-assertion harness (render vs fixture substrate, assert component tree — jewel 2's missing half; agent lane restored). **Read-tile landed as v0 — the first rung only, by author settlement**: viewer without interaction; drilling, mode switch, view-state, verbs, citizens, folding deliberately deferred until their foundations exist (registry match, slots, navigation). Not spec-complete and must not read as such. Tracked debt: per-program React bundling (~0.6MB) — exits are shell-injected shared runtime or projected-grade render-as-data (programs.md §3.5, round eight); decide in the citizens/slots era. **Spec-truth pass two complete** (Fable agent, folds grep-verified): every ruling from the surface/serving/depth stretch folded — programs.md (read-tile pins, menu-as-overlay-program), host.md (ol:// serving, executable base, identity channel, gray canvas, two depth registers, scrollbar pin, sidebar rules, radius convention), engine.md (R7 every-composed-accepts, federated unresolved intersection, residency, branch-merge ruling, attach load-bearing), sdk.md/db.md (wire truths, seq ties, residency, deferred marks), substrate.md (merge open updated), bootstrap.md (migration open, host programs, result-archetype rule). Honest residue, all marked open in place: engine's anchor-row bridge still in code (retirement queued); result-type *names* for tools need their own small pass; engine wire doesn't emit unresolved yet; useScope error channel; probe error channel. Specs and code agree again. **Queue**: owed jewels (fiber connector; probe error channel), session ratification read (author gate), VM ruling (agents gate). Tailwind landed + presentation corrections (author-accepted). **Author note, recorded**: read-tile rendering status-if-present off chunk bodies is interim hack — the real answer is the renderer ladder (a process chunk matched by its own renderer program, slotted in); resolves with the ensemble/citizen work. **BUILD TRACK PAUSED at the dispatch frontier (author ruling)**: implementation ran past the settled layer — a program launch is arguments/grants/forms/landing, and programs.md holds those as round-two bullets, not a drawn contract. Planning phase now: a fresh session stands on programs.md whole + rounds five–nine, works the dispatch agenda in author dialog (1 interface contract to build depth: argument roles, schema, grants→boundary derivation; 2 launch flow: palette→generated form→boundary chips→run/launch→landing, arranger vs auto-split; 3 one mechanism or three faces: palette/new-from/verbs; 4 VM tools from UI, inspector; 5 what citizens/slots will reshape), writes spec as rulings settle, then building resumes. This is board Next 1 finally taken at its word. Prior unit ruled: context menu** — deliberately simple: an overlay surface program taking (entries, anchor) as its argument — each entry a label plus the op it performs (run a program / cancel a process); menu executes the pick itself under the boundary its caller granted, then exits. Registry-generated entries (verbs from the field) remain the caller's future evolution — the menu program stays dumb. From here: processes runnable/terminateable from the sidebar, tiling basics after. **Settled progression**: (1) sidebar v0 landed — naked strip outside geometry, tree evicted (read-tile sole leaf), cards-vs-flat live, menu stub honest, 23 TS + 6 Rust tests; its eight flagged gaps (argument type unnamed in step 10, pending-has-no-form, no ordering rule, second-strip stacking unspecced, first-write/hide deferred) join the spec-truth pass; next: (2) spec-truth pass folding all accumulated pins + read-tile's eleven gaps; (3) owed jewels — layout-as-data, fiber connector; (4) read-tile deepened toward spec as the viewer pattern-setter. Then tab-bar/palette, then the agents-project gate (author rulings). **Author review rulings (window v0)**: context menus are NOT per-program — the settled verbs-from-the-field + host/overlay shape is the real design, landing with overlay machinery; interim menu component lives in @openlight/ui. Styling: no Tailwind — tokens as CSS custom properties + semantic components (Card/StripItem/Menu/Pill) in @openlight/ui; semantic names keep the probe lane meaningful; projected-grade future reduces CSS's role. Program loading direction: shell injects one shared React runtime (bundles shrink to program code); wry custom protocol later; projected grade remains the deep answer. **Standing steward duty**: visual inspection of the window (`./target/debug/host`, screencapture, read the image) belongs to the steward before the author — blocked this session on the macOS Screen Recording grant (requires terminal restart; do it between sessions). Read-tile deepening explicitly gated on the author's session/agent settlement (confrontation page + author-instinct in spec/research/). **Author correction, queued next**: sidebar is currently a tile leaf in the rim's demo tree — wrong; it is a naked surface outside tile geometry (host.md step 10). Next surface unit builds it properly (own strip webview, read roots [session, engine/process, engine/program], card-vs-flat per §Sidebar) and cleans the demo tree. **Absorption pass complete** (Opus agent, verified): union-accepts composition paragraph + per-list ambiguity + relates-uniqueness exemption folded into substrate.md, worked example rewritten turns-only; db.md absorbed the full drift list (relates in scope, exclude, tail-first pagination, fork materialization, Commit.branch, open_read_only, schema-version home) checked against code; sdk.md types synced; bootstrap.md fixed (tile/session propagate, relates-placed type definitions closing the resolve-empty trap, model/agent result types); host.md boot step 4 corrected. New opens it surfaced, deliberately not folded: **tool-program result types unnamed** (filesystem/shell/web/echo results and gate would reject under federated enforcement — naming them is design, needs a ruling or reasoned pass); `exclude` doesn't reach dimensions/edges (unruled divergence); `Includes.rank`/`snippet` declared but unbuilt; db.md layout-root path stale. Substrate pins awaiting author: **union-accepts ruled: union** (author: a chunk truly instance of multiple things is judged so, per role; merely-related content goes relates — matches `spec/research/union-accepts.md`, which confirms the built reading plus three sub-pins; fold the verbatim paragraph into substrate.md next session; seven adjacent fixes listed there, incl. a latent bootstrap trap where accepts names resolve empty and reject everything on session/tab under *any* reading, and the federation validation gap); then: relates exempt from name uniqueness (steward-closed, folding into substrate.md); **duplicate explicit seq ruled** — allowed, ties order by commit time (author-delegated discernment); **unresolvable scope roots** — no error, surfaced as `ScopeResult` metadata (`unresolved` roots; author's DX reasoning, refined from the error proposal); **branch ops ruled** (author): branches diverge freely; merge auto-takes the union of additions and fails hard only on true collision (same chunk's body/spec changed on both sides); no merge-conflict harness anywhere — the resolver is an agent reading both branches with existing tooling, committing the reconciliation as ordinary work. Substrate refuses, intelligence resolves. Folds into engine.md/db.md with the engine builder's landing. Mine-sweep closures (steward, pattern-applications not design): every program's interface declares its result archetype (generalizes the ruled model/output + agent/answer pattern to filesystem/shell/web/echo); exclude shapes results not dimensions (v0.1-as-built, honored in dimensions when a surface demands); rank/snippet marked deferred in db.md rather than overdeclared. **Author flag, standing**: session.md is agent-written and not author-ratified — round twelve's "author reads session.md fresh" test remains outstanding; the agent.md-pause contradiction and session-rethink P1–P7 all block on that read, not on mechanical strikes; agent-experience work stays outside the build track's settled-layer boundary until it lands. Risk probe ran (ten findings); gates sequenced: **before SDK** — db.md/sdk.md absorption pass, wire-shape pins (run-args naming, get opts) with shared wire fixtures, rim dispatch seam reshaped async (sync today; engine.md's engine is not — cheapest while the trait has one implementor). **Before swap** — per-project bootstrap seeding as an explicit step; name-lookup convention (surfaces reference string ids; substrate generates ids — tracked debt now load-bearing). **Before agents project** — VM containment ruling (no macOS mechanism specced anywhere: Virtualization.framework image vs a marked subprocess escape hatch) and the agent.md-pause vs session.md §1 contradiction. Engine conformance fixtures (engine/fixtures/, same case format) wanted while the harness pattern is fresh. Toolchain: Homebrew rustc 1.86, `time` pinned 0.3.41 in lockfiles; root workspace assembly is a deliberate later step; substrate.md's session example still shows deleted event types (edit at next touch).

**Session rethink — fresh-context pass on "what is a session" (`spec/research/session-rethink/`).** A deliberately clean session, per author request, took session.md's turns-only model as revisable and circulated from first principles. Method: own pass written down first, then three parallel probes — a blind derivation from the four mechanism specs only (the cleanroom recipe), an adversarial UX attack, a generative pass over the citizenship space — synthesized in `synthesis.md`. **Nothing folded into the specs; all proposals await author rulings.** Found and verified: **agent.md's pause section contradicts session.md §1** (promises "discuss the work in the conversation itself"; turns-only forbids any message in an agent session) — must be resolved either way. Four-way convergence: (a) **the human voice below the model call is the one amputation** — turns-only is machinery discipline misread as content law; proposal P1 admits a `note` kind (recorded, never completes), settles one-archetype (utterance kinds — turn/note/control — not container kinds; dispatch is a property of the gesture), corrections `relates`-anchored on the answers they correct; (b) **the session conflates stance / trail / face** — the blind pass, seeing only mechanisms, derived the *stance* (a standpoint: pinned selection + accretion) as the essence; P2 re-centers §1 accordingly, with curation-outward as the long arc ("the transcript is the receipt, the field is the asset"); (c) **the face must not default to the raw trail** — P3 digest-first resting fold (Worklog-branchless as v0.1 default; §7's "reads as a conversation" marked staging, challenged), P4 standing-brief chunk pulled forward (one chunk: list-name, re-entry card, fold face, widget, context projection), P5 artifact-as-ground for working sessions written in as destination (*you watch the work, not the log*). Also: P6 sessions found from the field (needs-you inbox and mission-control as scope queries; fresh evidence for gate-placed-onto-session), P7 typing-while-running = pause+adjust in one gesture. Seven buildable session forms in `forms.md`, none needing new mechanism — the citizenship spaciousness claim survives pressure. Turns-only's machine half (frames, context purity, derived status, reconstruction-over-storage) survives the attack intact and is defended in `critique.md` §4.

**Rebirth, round thirteen — review pass; black spots closed.** Self-review found no architectural cracks; unfinished corners fixed without new authority needed: bootstrap/agent.md/programs.md reconciled to session.md's turns-only model (bootstrap was still seeding the deleted archetype); session.md gains §7 conventions — completed turn rests as prompt + answer visible; session defaults as field entities; controls carry targets (+ `abort-completion` signal); `adjust` = instruction block, audited; `[[chunk-id]]` mention syntax; include-the-session default (recent full, older summarized); failed turns as first-class resting form (error + mutation strip + retry/review); per-turn cost visible. Open: removing past turns.

**Rebirth, round twelve — the session experience specced (`spec/session.md`).** Grounding continued to the felt experience, per author: an agent session is **just turns** — the prompt is an argument on the invocation, the answer a chunk on the frame; no message/tool/context events on the session (messages belong to human conversations — one-vs-two container types open). The **turn renderer** split from the generic invocation renderer (matched specifically on agent processes — a process is instance of its program, so the existing ladder carries it): folded = a live status line *derived* from frame children and commits (cannot lie); expanded = the thread of reasoning (prompt + pinned chips, cycle segments with thinking folds and clustered parallel tools, mutation strips, audited context deltas, gates, streaming answer). **Thinking visibility resolved factually**: server-side thinking *streams* — the model program committing throttled deltas through the R6 pipe is the whole mechanism; recommended pulled forward from deferred (it is the felt difference; ChatGPT-vs-Claude-Code gap named by author). Three layers: derived status → streamed thinking → narration as the calm default reading. Cycles sequential, in-cycle tools parallel, one-active-turn as dispatcher policy. v0.1 implementation order in session.md §6. Next: author reads session.md fresh — the "do we know what we're building" test — then branch ops, then the db crate.

**Rebirth, round eleven — the conversation grounded by walkthrough, and the epoch closed.** A full working-session simulation (agent morning on a conversation, event by event) grounded the dissolved model, then author corrections landed it: **context and discourse named apart** — the agent's argument is an ordered *context list* (guidelines, conversation, prompt, per-turn additions), living as context items on the invocation's frame; the *conversation* is only the discourse scope — container-typed (`{ordered: true}`, content wildcard, no agent-session type), where messages and **invocation entities** (the turn's process chunk dual-placed with seq) accumulate. History stays linear while context varies per turn — both recorded. Provider-API coherence is *reconstruction from frames as serializer policy*, not stored event types — `tool-call`/`tool-result` conversation events deleted. **Summaries are groups**: a summary chunk placed on the entities it abstracts *is* the group (the intersection pattern), killing the range-reference mechanism before it was born. Dispatcher = one citizen (compose + launch + selection); selection is a field entity the reader highlights from — citizens coordinate through the field, never props. `converse` struck from the program set. Open: gate placement (frame-only vs surfaced onto the conversation mid-run), participants-as-inviters, selection expression. **Epoch note (corrected by author):** not closed — *stopped by exhaustion with the connections unassembled*. The insights are lifted; what's missing is seeing them whole. Round twelve added the answer-home rule: the agent's discourse target must be a `conversation` instance (the agent session's typed descendant) — the dispatcher matches `conversation` and nothing else, so an integrated email thread never grows a dispatcher (its own composer answers in its own medium; *talk about this* bridges into a conversation); the context list stays wildcard — anything can be context, only a conversation can be the answer-home. **Next is not implementation: a consolidation pass** — rewrite `programs.md` + `agent.md` as one coherent statement of the settled model (no new design, pure integration, opens marked), for the author to read fresh and judge whether we know what we're building. Then branch ops, then build.

**Rebirth, round ten — the demand ledger folded into the mechanism specs.** Delegated by the author ("beyond my scope, proceed"). The two spec bugs fixed: read-only-mount enforcement now rejects *modification of records resident in* a mount, never mere reference (engine.md — the federation pattern depends on it); trace nesting exempt from typed `accepts` (child processes validate against `engine/process` only), plus a new invariant: terminal cleanup never severs the frame (recipes, autopsies, re-runs depend on it). Protocol landed: `cancel` (descendant-or-write-boundary authority), `run` modes `child | launch`, `exit` (webview self-dismissal), `read_batch` (tagged sub-queries, per-identity authorization, one snapshot), `dry_run` on commit, `results_only` on await, whole-field FTS + `exclude` negation, pagination/body-less probes settled in substrate.md, timeout pauses during child await. Streaming sanctioned as convention (throttled partial commits; subscription coalescing promoted to required). Capabilities/secrets enforcement written (provider-enforced vocabulary; secrets as env vars from the host keychain, never chunks). Per-slot identity in host.md/sdk.md; boot-suite boundaries specified in host.md step 10; overlay escalation leaning arranger-mediated; recipes settled identity-based; ensemble tile and host-native-sidebar recorded as host.md opens. Branch ops (R1) shaped in engine.md's open list — the one deliberately not landed, pending merge-semantics design. db.md absorbs its share (pagination, batch, branch plumbing) during the db implementation pass.

**Rebirth, round nine — slot-and-hook consolidated; technical descent stopped by choice.** The embedding exploration (rounds six–nine) consolidated into one block (programs.md §3.5): a slot is a scope offered as invitation; matching is SDK-root infrastructure, never the parent's code; a citizen's needs are its own, declared via hooks at any depth, beyond the invitation if needed; resolution is coalesced — one normalized deduplicated query per render pass at one commit snapshot, slices projected to hooks, touched-set-targeted re-renders; parents pass addresses, never content; embedding never escalates (citizen ∩ embedder cap), which makes the projected/sovereign grades a privacy decision. New demand: coalesced multi-identity read (§6). **Declared open deliberately** — the rendering deep-dive (per-level batching, React-inside-iframe replicating the resolver per realm, static need declarations as waterfall cure) and the cache's home (lean per-host) were cut short to conserve the session, not because they're answered; they settle by building the conversation tile.

**Rebirth, round eight — projected vs sovereign (render-injection understood).** The author's React proposal, clarified, superseded round seven's iframe-vs-React framing: the program stays a program to the engine; its *render output* crosses into the embedder — code and credential never do. Folded as leading candidate (programs.md §3.5): **two embedding grades** — *projected* (default: pure render = f(scope), executed by a host-side projector that resolves scope first and renders once with data in hand; UI-as-data injected at the slot; interaction via declarative intents executed under the citizen's engine identity) and *sovereign* (exception: confidential interaction like the composer, live execution, untrusted code — realm boundary with per-slot identity). Cost collapses — projected citizens are render calls on commit, not realms or resident processes; minimized-forms-as-data was this grade's first special case. Open: UI-as-data shape (neutral vs React-element), purity enforcement (read-only handle, intents as only effect channel), `projection` as a third runtime kind — resonant with projection-as-one-primitive.

**Rebirth, round seven — library vs citizen; arrangement ownership opened.** The iframe-vs-React question dissolved by definition: in-realm composition is *library use inside one program* (always allowed, never citizenship); a citizen is another program, and programhood needs a realm boundary to be enforced rather than advisory — so expanded citizens are sandboxed iframes (DOM-streams later), React is the compromise, not the iframe. New: **minimized forms are data, not surface** — a minimized citizen writes its minimal living state into its frame and the widget stack renders it from the field (controls are chunk writes); only expanded citizens cost a realm, and the widget stack is owned by no one. Ground-owns-slots demoted from proposal to *open*: candidate split is a shared arrangement layer for medium-independent placement (uniform across views, person-governed) with grounds *offering* medium-dependent anchors (reply-at-message) into it — needs examples beyond the conversation to settle; converse arranging itself first harms nothing.

**Rebirth, round six — surfaces embed surfaces (requirement settled; slots proposed).** Author grounded the peer inversion's missing half: composition can't belong solely to the host's split — a surface must render other programs within its own layout (the composer pinned at the thread's scroll-bottom, shrinking to a widget when scrolled away; an email reply rendering *at the message* via its reply verb, not in a bottom split). Requirement settled; mechanism proposed and marked leading-candidate in programs.md §3.5: **slots** — embedder declares in-flow / pinned / widget positions and governs geometry, never content; every embedded citizen stays its own process (own boundary, own commit attribution); ground citizen provides its medium's slots, host provides fallbacks; respect gains two rules (embedder can't observe embedded content; embedded can't escape its slot). v0.1 material candidate: sandboxed iframes with **per-slot transport identity** — new demand in §6 (host.md + sdk.md), beside the ensemble-tile demand. Revises the clean-room "surfaces never nest in the DOM" constraint, deliberately. Open: slot protocol, performance ceilings, minimal v0.1 cut (pinned + in-flow in `converse`).

**Rebirth, round five — the peer inversion (direction under discussion).** Author caught the residual asymmetry: thread and composer register for the same scope type — host/guest between them is app-thinking. Proposed shape, marked as direction in programs.md §3.5: **the tile is scoped, not programmed** — all programs in a view are citizens, peers ontologically, distinguished by *role* (ground / dock / widget / dormant); the thread is just the citizen holding the ground. Evidence it's found: thread-minimized yields a quick-dispatch tile; narrate-as-ground yields narration-as-default-view with zero mechanism; per-citizen boundaries sharpen trust (viewer can't write, composer can); "opening X" unifies with recipes (a view recipe per archetype). Open: arrangement grammar (roles vs subtiling vs both), must the ground be occupied, and a named host demand — tile leaf relates one process today, an ensemble needs a group or subtiles. Settles by building the conversation tile as the first ensemble.

**Rebirth, round four — citizens become a core mechanic.** Rulings: the **composer is a citizen** (the conversation's input chrome joins the same way rendered citizens do); the **model family is capped at the canonical interface** (adapter lives in the model program); and **citizens generalize to every viewed scope** — compatible set from the registry match (same match as verbs-from-the-field: momentary vs resident presence), three states (dormant / minimized-with-living-controls / expanded), persisted in view-state, **governed by the person with respect** (invited, never seizing; expansion is the person's gesture; dismissal always available). Visual form (icon widgets bottom-right, manager at bottom) recorded as an open sketch, not settled. Under discussion, proposed and marked open in the specs: **participants as relations** mark a conversation as agenting and invite its citizens; **the conversation slims** — mechanics live on frames, the conversation references them (discourse only: message/gate/control), which would dissolve the agent session entirely. Directive logged: agent.md wants a deeper research-based pass before implementation (board *Next* 0).

**Rebirth, round three — openness made legible.** New working agreement ([`conventions.md`](conventions.md), *Build on ideas together*): open ideas are reasoned through in conversation before folding; specs mark openness in place (*Held open* / *Open* / *direction*) so explorations can't read as decisions. Corrections from author review: sidebar/tabs back to surface programs (frame machinery stays native; going native held open); launch gains a **Uses line** — the programs a program runs, beside capabilities; folds expand in place within the conversation (tile optional); narration marked as *direction* — a calibrated first-class default view mode eventually. Marked open, deliberately unsettled: **where provider adaptation lives** (agent-reads-schema vs canonical-request-with-adapter-in-model-program — leaning the latter; settle against the second provider built); **the conversation's interaction chrome** (pluggable composer vs thread + dispatcher recipe — settle by building the first two conversation kinds).

**Rebirth, round two.** Author review of round one (minor honing edits throughout, plus rulings) folded in:

- **Argument coherence** — one argument chunk per role, keys within it; a second type only for a genuinely different kind of thing. **Demand refined into grants** — schema fields marked `grants: read|write` derive boundary roots from filled arguments; static demand covers only the argument-independent residue. **Levels of abstraction** made explicit: multi-chunk anatomy is substrate-level; concepts render and behave as one concept.
- **Frame machinery is host-native; sidebar and tabs stay programs.** Window, tiling, background rendered in Rust (performance and feel; native-graphics tiles reachable); sidebar/tab-bar remain webview surface programs naked on the background — going native later is held open against visual coherence vs current demand. (Round two misread this as full-native chrome; corrected in round three.)
- **Engine-as-daemon** named as the settled direction (new horizon entry): hosts as attaching windows, no state drift across windows, network reach, a home for daemon programs. v0.1 keeps the engine linked; the protocol seam preserves the path. **Daemons (services)** acknowledged as essential to a functional compute environment — one-shots vs daemons named in programs.md §2, lifecycle extension post-v0.1.
- **Conversation is the broader primitive** — an agent session is just a conversation an agent participates in; modular inline rendering (entities at natural representation; third-party event types join without touching agent code); *talk about this* from any entity. Session archetype renamed `conversation` in agent.md; prompts/answers unified as messages (bootstrap.md sync pending).
- **Verbs from the field** — entity context menus generated from the program registry (programs whose argument types accept what's at hand); clipboard as an ordered chunk with native host integration; popup-at-point → expand-to-tile.
- **Pause over cancel** — a `control` conversation citizen halts the loop between cycles; inspect and discuss while paused; **context purity**: meta-discussion stays out, only distilled adjustments enter the next cycle. Cancel remains for killing.
- **Model plurality** — `model` is a family, not one shape: each provider its own program declaring its request/output schema through the standard interface chunks; agent is provider-generic by reading that schema. **`narrate`** added: live model-driven abstraction with summaries as linked chrome; folding as navigation.
- **Launch forms generated by default, authored when wanted**; capability/secrets held open as possibly one family with integrations. **Languages clarified** in conventions.md: Rust platform, TypeScript for pilot programs + first SDK, programs not limited to TS.

Pending from this round: fold native-frame into `host.md`; sync `bootstrap.md` to conversation/message types; author review of `spec/` files (not yet read — trusted forward).

**Rebirth epoch.** The tainted value layer was ditched and the program layer rebuilt from clean ground. Method: the author's plain statement of what OpenLight is + the four mechanism specs were given to three blind passes (fresh contexts, forbidden from reading the value documents); their convergence rebuilt the program layer ([`spec/research/cleanroom/`](spec/research/cleanroom/), synthesis in [`cleanroom/synthesis.md`](spec/research/cleanroom/synthesis.md)). Changes:

- **`inside.md` deleted.** The author judged its values slopped in exhaustion; the clean-room evidence agreed (nothing concrete demanded its heavier claims; the band's mechanical content survived without the mythology). No values file for now — one may be re-earned later, from what the programs demonstrably demanded, if wanted at all.
- **`README.md` rewritten** around the center: *completion from a point in the field, not from a pasted transcript.* Trust mechanics are native and valuable — and secondary.
- **`horizon.md` rebuilt** as the vision file, by proximity: integrations (external schemas projected into the type system as live chunks, temporal inference included), cache embodiment (substrate on the model's cache; all context windows are always the scope), authoring-here-shipping-out, the interface-inference ladder, the band (kept deliberately, marked least proven), plus the surviving engineering directions (uniform containment, peering, view modes, WebGPU).
- **`spec/programs.md` rebuilt at experience depth** — the call frame, the two run modes, sidebar/palette/launch/read-tile/inspector specified to click level, the program set, demands with rulings.
- **`spec/agent.md` rebuilt** on the model/agent split: `model` as the single provider seam (frame-only boundary, secrets never chunks, verbatim request as artifact, usage as a query), `agent` as the harness (context items pinned and `relates` on sources — consumption tagging and reproducibility native). Harness may decompose further.
- **`conventions.md`** made self-contained (principles folded in, short). `spec/bootstrap.md` updated (`model` + `agent` programs, `gate` event type).

**Author rulings recorded** (from the rebirth session): completion-from-scope is the center, trust secondary. Lifecycle: surfaces are viewers — `run` (child, cascades) vs `launch` (detached, session-parented); a launcher's death never owns the work's fate. Program interfaces described delicately across multiple chunks (argument/result/demand; schema documents all keys, `required` marks the mandatory). Streaming as throttled partial commits, coalescing required. Recipes identity-based. The band stays on the horizon — too novel for blind derivation, not struck for it.

**Demand list pending.** [`cleanroom/synthesis.md`](spec/research/cleanroom/synthesis.md) §5 holds R1–R12 against the mechanism specs — two spec bugs first (R5 read-only-mount wording, R7 trace-nesting vs typed `accepts`), then branch ops (R1, unlocks agent-on-branch → review → merge-as-acceptance), pagination (R2), cancel authority (R3), whole-field FTS + negation (R10), capabilities/secrets (R8), and the rest.

**Prior epochs (condensed history, still true of the mechanism layer):** spec phase settled as v0.1 — db, engine, host, sdk drawn whole, cross-referenced, stress-tested (six review subagents), refined through the peering/federation/runtime-registry pass; multi-project mounts first-class (read-only, cascading, branch-pinned); directory restructure (top-level first-party projects, `.ol/db` + `project.toml`, one SDK package); db.md Rust architecture drawn (~2,300 lines estimated); reactivity end-to-end settled; runtime kinds via engine registry (`webview`, `vm`); compositions as the island system. The clean-room passes independently confirmed this foundation holds under blind adversarial use — every finding was a protocol addition or wording fix, not a redraw.

