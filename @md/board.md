# Board

The working board — current state, the queue, the gates, the debt. Session narratives live in [`log.md`](log.md); exploration records in [`spec/research/`](spec/research/). Updated as things move.

---

## Now

- **THE SURFACE REWRITE LANDED (2026-08-21/22).** The spec tree rewritten whole from the brief, every file through the audit cycle (loss + honor, findings folded before commit): engine.md · substrate.md · chassis.md (was host.md) · view.md + components.md + desktop.md (programs.md dissolved along the module structure) · bootstrap.md · sdk.md · pilot.md; horizon/db/agent/sketches swept; conventions.md rewritten same sitting on the week's knowledge-structure learnings (records are events · outline-before-prose · name what you leave out · context craft). **The author's ratification read is now the standing owed act** — the brief retires into git on its completion (proposed, not ruled). The author's stated order (2026-08-22): view.md, sdk.md, components.md first. Queued cheap-session: the conclusions.md audit-and-close, the agent.md deep-claims sweep, the fixtures. Narrative in [`log.md`](log.md).
- **THE FEEDBACK FOLD (2026-08-20), corrected same-day on the author's review.** Five author notes on the ratified brief: sources engine-served [R] · the mount context became the **call context** — the engine never names view [R]; the contract's shape (declaration + argument + grant + parent; `Context { anchor }`) is steward-drafted [P], with the process **frame** (cap-exempt) named as the generic contract's open seam · the home is a **standard store directory** (`.ol/db` + `.ol/project.toml` — established naming kept, contents superseded; `field.ol` was the deviation; `--home` may point at a project — the project-agent line cashed free) · the **simple tiler** — tabs and sidebar out of the pilot shell [R]; the verb set (split/close from the match, resize the split's own gesture, move/swap post-pilot on the drag layer) steward-drafted [P] · **the session ruled: minor state of the pilot desktop module** — the engine's `launch` owner is configured; two duties earn it (the writable indirection under a read-only shipped entry; the work group for kill-cascade). The review turn: the steward had skipped its own named dialog, minted the tiler verbs, and over-recorded in `conclusions.md` — retagged, corrected, the conclusions entry trimmed to a pointer. Closing sweep (same day): **daemons ruled** (ratification 3.2 — impure programs launchable like any, purity never the gate; nothing auto-starts, user-started services, the honest face when absent) · the knowledge-structure question (7.1) recorded as a **marked-open point in `conventions.md`** · **`ratification.md` removed** after its cited rows (3.3/3.4/5.3–5.6, plus 5.1/5.2's reader drafting material) were restated inline in the brief — one source of truth; git keeps it. Narrative in [`log.md`](log.md).
- **THE SURFACE ARC — RATIFIED (2026-08-13/19); the brief stands, the rewrite is next.** Ratification ran over the author's read and a deliberate check-in fold that re-derived the interface model one level simpler. **One file stands: [`spec/research/surface/proposal.md`](spec/research/surface/proposal.md)** — components ↔ programs, mounts ↔ processes (a mount is a call: `component` + `argument` as a selection, matched by the match); surface as the drawing target, hosted and glued; realization = implementation | template; three selections down the mount tree (reach, `draw`, `offer`); acts ordinary and `launch`-only from the interface; input as data; the engine its own artefact with one transport object; the attach record one shape everywhere; the chassis declaring its entry contract; the home (`ol.toml` + `field.ol`); the desktop module with the shell as a template. Record: `ratification.md` (closed; removed 2026-08-20 after its live rows folded into the brief — git keeps it); grounding: [`act-scenes.md`](spec/research/surface/act-scenes.md); ruling recorded in [`conclusions.md`](spec/research/arc/conclusions.md) (*The surface arc — ratified*). Prior generations deleted (git keeps them). **Nothing folded into the spec tree yet.** Narrative in [`log.md`](log.md).
- **THE BATCH LANDED (2026-08-07/08).** The worklist's absorption pass executed across the tree: the law is now the specs again, with nothing standing superseded-on-paper. `session.md` dissolved into `agent.md`; `pilot.md` slimmed to what is v0.1's own; the vocabulary ruling swept every file; **substrate.md rewritten on the dimension law** (reach = boundary selections, ownership demoted to naming and one-hop membership, uniform filtering, counts describe what your boundary admits); **engine.md rebuilt** on it (boundary as a selection expression, the frame as `[self]`, `accepts` and the match, the argument a set-valued field, `resolve`, the planner partition, buffers, containment); **host.md slimmed to Rust authority** with the arrangement layer moved into **programs.md**, which took the seated argument, the reader's `list<selection>`, grades and the beyond-reach face; **db.md** made it physical (boundary evaluation in the read path, `seq`'s schema and its auto-assign join, the plan interface, the three budgets); **sdk.md** put it on the wire (selections, the `$type` tag, uniform counts). Session narrative in [`log.md`](log.md).
- **THE SMALL-RULINGS CLUSTER CLOSED (2026-08-12, same sitting as the seam rework).** Every batch-raised gate ruled and folded in place — see Gates for the roll-up. Standouts: **the tile tree became typed fields** (grain rule; `seq: true` retires from the arrangement; leaf = the process; one-hop `[session]` for the writers; union ref-constraints admitted), **slot preferences are reader-owned** (the exception *is* the pin — the seat made explicit; shape-rules as collation settings), **single-request is derived by the planner, never typed**, and **temporal queries entered v0.1 scope**. What still gates anything: the seat model for the boot suite (bootstrap's last blocker), `follow`'s drafting note, the buffer realization, VM containment before the agents project.
- **THE MODEL SEAM REWORKED on the author's first review (2026-08-12), folded same-day.** Reviewing the E diff, the author caught outbound coupling and dissolved the request wholesale: **`model` takes a selection** (the offer is the grant; serialization is deterministic provider code; **the argument is the consumption record** — `includes` retired unbuilt); **one canonical `model/output`** shared family-wide under a new `model` dimension; **tool calls are drafts composed by the model program** (`calls: list<ref>`; the agent starts within-walls drafts, beyond-walls rest — escalation is a resting state, not a path); **derivable-over-verbatim blessed as first-class**; **`selection` became ordered** (`list<loc|ref|expr>`, duplicate clause; order = window order, composition-visible); never-widen wording corrected across engine.md/programs.md. Ruling in [`conclusions.md`](spec/research/arc/conclusions.md) §E addendum. **One steward lean awaits the author**: the frame's read term as `[self] | follow(owned)` (engine.md, marked in place). Author closed the agent scope: *"we now have settled a good foundation."*
- **E RULED AND FOLDED (2026-08-08/11) — the worklist is finished.** The agent sitting ran over the steward's position paper ([`spec/research/arc/agent-position.md`](spec/research/arc/agent-position.md)) and four dialog rounds; ruling whole in [`conclusions.md`](spec/research/arc/conclusions.md) §E. The spine: **context = the argument** (`accepts: [selection]`; no context structure exists); the record as intent (frozen argument expression) and fact (the model runs — reshaped 08-12, see above); **`run` as the third boundary key** (toolset = run boundary; `model`/`web`/`filesystem` are enforced leaves; caps compose); **run-to-draft escalation** (approve = start with the approver's reach; deny = cancel → `failed: denied`; **`gate` is dead**); controls via the context menu with purity as a checkable delta list; include-everything-normalized assembly with no silent reduction; flat sub-agent citizenship. **agent.md rewritten whole** (dependency-ordered, four deliberate opens); engine.md folded (`run` key, run-to-draft); bootstrap's agent seeds written. UX grounded scene-by-scene in [`composition-scenes.md`](spec/research/arc/composition-scenes.md).
- **THE INTERFACE SITTING (2026-08-12, provisional — "acceptable for now, learnings down the road").** Overlays and the command surface settled through dialog + [`menu-scenes.md`](spec/research/arc/menu-scenes.md): **the base page is host code** (root slot + overlay layer; renders, never decides; the root program decoupled); overlay = `{anchor, content, grade?}`, fields only, **grade chosen at dispatch**; gesture split (secondary = seat machinery → intent to anchor-holder; ordinary = the surface's); **entry grain = payload archetype**, menus assembled from field reads (registry match + declared `actions` at two scopes + derived choices); **one command program at two grades** (name open); **`board` → `arranger`**, landing as `arranger/open` with `position` value chunks, starting/landing two acts; the context-menu/value-select line; anchor inheritance. Ruling in [`conclusions.md`](spec/research/arc/conclusions.md), *The interface sitting*.
- **THE SEAT MODEL RULED (2026-08-12) — bootstrap.md has no markers left.** One way: the host boots the shell alone; the shell's chrome seats are **pinned** — `sidebar([session])`, `tab-bar([session])`, deliberate seats, no match, no swap — each chrome piece its own program, started as the shell's children; the palette host-started and host-placed as a session overlay. Accepts are bare session refs (`request` payloads die). The surface seeds are written; `collation` seeded under engine, `reading` under the reader. *Direction, later:* root-as-pure-tiler with chrome as choices — not the pilot's.
- **Ratification state**: the author has read the E-batch *diffs* of engine.md, view.md, substrate.md and agent.md (2026-08-12 — corrections folded same-day, above); the full files, the 08-07/08 batch, agent-position.md, composition-scenes.md and conclusions.md remain unread. The full ratification read is still owed and is the next author act.
- **Specs lead; code follows.** The mechanism layer (db crate, engine ~6k lines, TS SDK, host on the real engine) is green against a spec generation that is now two rewrites old. The alignment pass is the next build phase; divergence pinned under Tracked debt.
- **Surfaces standing**: read-tile v0 (viewer only), sidebar v0, context-menu overlay program, tiling with real verbs + hide, telemetry + warm-open recycling (~50ms). The telemetry/warming stretch is **unreviewed and distrusted by the author** — review still queued.

## Namings — pinned for code

*Pinned before the surface arc — **historical since 2026-08-22**: the rewrite landed, and the spec tree is the naming authority now (view.md, components.md, desktop.md carry the live vocabulary; the supersessions listed below still hold as history).*

`reader` — the viewer program: renders nothing, merges nothing; serves a reading, hosts its collation's selections side by side · `reading` — the persistent store the program serves; its body's `current` ref moves to the collation in view · `collation` — one immutable value: an ordered `list<selection>` plus settings and a predecessor (branching free; a value) · `selection` — the type word: `list<loc | ref | expr>`, ordered, duplicates rejected; a collation member is a chunk, a place, or a pure derivation · `location` — one place: an intersection of place terms · `follow` — closure-of-a-step, pipe verb beside `at`/`where`/the hop verbs; "lineage" the prose noun; parameterization landed (engine.md, *Hops and `follow`*) · `hop verbs` — `members` · `placed` · `owner` · `refs` · `backrefs`, plus `prop` (single-key body projection) · `sequence`/`table`/`document` — the ground surfaces · `process-view` — the process surface: argument · frame · result · `prose` — markdown-with-slots; mentions render through the ladder · `seat` — what mounts and what confers chrome and priority; a tile tree, a collation and a slot are the same kind at three altitudes · `grade` — the size dimension of matching · `lift` — giving a value identity (renamed from `select`, which collided) · `draft` — pre-start process state; argument editable iff unconsumed · `the match` — the start-time argument check · `arranger` — the tree's command writer (renamed from `board`); its payloads are the verb vocabulary · `base page` — the host-code skeleton: root slot + overlay layer · `actions` — the program-body key declaring scoped verbs (render-chain scope and offer-type scope) · `run` — the third boundary key: the programs a run may start; the toolset · `run-to-draft` — the escalation mechanic: exceeding the walls composes a draft; approve = start, deny = cancel. Superseded en route: gate, request-and-`includes` (the model takes a selection; the argument is the record) (approval is run-to-draft; a question with no action is discourse), lens, focus, trail, eye, position, preset, template, marks, contribution, tab-language, binding-as-tab, signature, piped, scope, dispatch, invocation, demand, ordered, `form` (dissolved into the seated argument), `member` and `binding` (collations hold selections), `select`.

## Next

**0. The surface rewrite — the spec tree from the brief** (a frontier sitting, by the batch's method: the brief is the spec's source; fold whole sections, not patches). *The session talk-over is closed (2026-08-20: minor desktop-module state); no design item gates the rewrite.* Order: **engine.md — LANDED 2026-08-22** (rewritten whole from the brief; both audits passed — honor: ~45 statements, 2 tag fixes; loss: ~180 claims, 3 minor losses fixed, 0 silent mutations; outline at [`engine-outline.md`](spec/research/surface/engine-outline.md), retires at cycle end) → **substrate.md — LANDED 2026-08-22** (surgical: the attach sweep, instance-removal ruling, ref-refinement open) → **chassis.md — LANDED 2026-08-22** (host.md reborn: the chassis + the home; combined audit passed) → **view.md + components.md + desktop.md + bootstrap.md — LANDED 2026-08-22** (programs.md dissolved along the module structure — view/ contracts + glue, component/* packages, the desktop module; both audits passed, all findings folded; files renamed to match the modules) → sdk.md (`engine/sdk` / `view/sdk`, one transport object, adapters) → pilot.md (the pilot cut, the home, the repo tree) → horizon.md additions → the mechanical sweeps (native/program verb, `over`, faces, `attach`, `surface` re-meant). *The ctx/adapters prototype gate is relaxed [R — author, 2026-08-21]: no standalone spike — resolve by making, aware; the first few components are the evaluation, and the component family does not scale before it happens. The brief's open-index line ("to prototype before folding") is superseded by this ruling; carry the [P] tags into the spec and settle them at implementation.* Then the board's gates roll up and the alignment pass plans against the new tree.

1. **The author's ratification read** of the rewritten tree — substrate.md and engine.md first, then agent.md (fresh from E), since everything else was written against them.
2. **The small-rulings cluster and the seat model are closed** (2026-08-12; Gates and Now) — the spec has no owed design items; only fixtures and the ratification read stand before the alignment pass.
3. **The follow note — LANDED in engine.md** (*Hops and `follow`*, 2026-08-12): six hop verbs + `prop`, follow as closure-of-a-step with depth and visited-set termination, closure output carrying its edges (wire shape at the SDK build), the planner lists updated, boundary-grammar sentences retargeted to "what lowers". Steward-drafted under the technical convention; rides the ratification read.
4. **The reconcile remainder** — cheap and queued: `pilot.md`'s two stale lines (chassis.md's description, `form` in the program list), the vocabulary rider on `horizon.md` and `sketches.md`, and the fixture files (`db/fixtures/*.json`, `engine/fixtures/boundary.json` — the last encodes the dead boundary model verbatim). *Plus the `conclusions.md` question (author, 2026-08-20, not ruled): for the surface arc it held nothing the brief doesn't; proposed at the rewrite — audit every live item into the board or a marked-in-place spec note, then close the file as a historical record.*
5. **`reader` v0** — the collation over the built intersection grammar, selections side by side, slot chrome, hide/show. Rename lands here.
6. **The seated argument** — the draft frontier's first unit; editability boundary-derived.
7. **`process-view` v0** · **`prose` v0** (mentions as links first, slot-grade when the ladder can inline).
8. **The context-menu/palette program** — one family, two anchors (session/point); underspecified and central; its own drafting unit in view.md's deepening.
9. **Review the telemetry/warming code** (standing; author distrusts it).
10. **Later**: integrations sketch (driver contract) · content-type register · well-known key vocabulary · fold/summary UX.
11. **The register pass** *(direction — author, 2026-08-22)*: the spec tree is engineering-heavy; a future pass re-shapes it top-down — laws, motivations and the non-engineering level first, each file an ease of introduction folding engineering where needed. Not this cycle's work; the surface rewrite leans headers law-first where free.

- **Remotes need content addressing** ([`spec/research/arc/object-model.md`](spec/research/arc/object-model.md), 2026-08-09, author-driven, not ruled, not v0.1). The data model is git's; the storage is a mutable page store with history reimplemented in tables. Invisible locally, fatal at a remote — without hashes there is no *do you have this*, so no delta sync, no dedup, no verifying a peer's claim. Sync then needs no subsystem: fetch is a read, divergence is the existing diff, pull is the ruled merge law across space. Cheap move justified now: **content hashes on chunk versions and commits** (a column and a function; needs canonical body serialization). Do not harden mounts as the only store boundary, or branch ops before merge-as-sync is understood. Buffers and v0.1 demand none of it.

## Ruled — closing stretch of the thread dialog (author's word given)

- **Indirection adopted** (the viewer-state dilemma): a process argument is an immutable *reference* to a chunk holding the whole view definition — scope expressions (the "DSL") *and* view settings. Edits are commits on that chunk; the subscription re-reads; and because the reference is stable, **the program can be restarted whenever** (recycling-friendly) without losing its view. Processes never mutate; data chunks live; records pin commits.
- **The dissolution transmutations adopted** — author: "that is the whole point." Answer-home → draft-summons-form; the conversation container dies; a conversation is a named location, materialized as a chunk only when named/shared/bound/peopled. The session.md ratification read is now a confirmation pass over threads-dialog.md.
- **Drafts are substrate-resident** — author: "there is no in-memory markdown; substrate it is." The draft process holds its draft prose in the field by the laws of the system; persistence is not a policy but a consequence. Nothing auto-sweeps.
- **read-tile → reader rename confirmed** at Next 3.
- **The naming family settled** (three-subagent exploration → textual-criticism register → author folk-simplification): reader · reading · collation · location · binding · follow (template and marks later dissolved — collations-as-values and slot chrome). Rejections that shaped it: braid (complecting — not the phenomenon), film register (spoken, not professional), loci/stemma/exemplar/sigla (right meaning, not folk).
- **Template/preset dissolved** (author): collations are values, so opening any collation — including one a program relates as its shipped view — is a fresh reading pointing at it; the first edit branches. Nothing is copied; no mechanism exists.

- **Stale-display default** (author direction): a changed-or-removed argument chunk still renders as-it-was in the process-view, marked stale — derived from then-commit vs head, cannot lie; generalizes unresolved-roots from *gone* to *changed*. Folds into the process-view contract. Narrowed: applies to the *content* a collation views, never the collation itself.
- **Collations form a citation DAG** (author, superseding the earlier sequence-of-focuses direction): each collation cites the one it was edited from; editing from anywhere branches; no pointer, no stored sequence, no deleted futures. **Collations are values** — identity captures version; references never go stale; nothing needs commit-pinning. The reading's `current` is a typed body field — the relates workaround retired with typed bodies (superseded note; settled.md §4). Open: how the predecessor citation is carried (relates is untyped — connection-chunk candidate vs typed-JSON ref); what is navigation-grade.
- **`at` is a pure pipe verb** (author): `scope | at(commit)` — time travel as DSL composition; vocabulary, no longer load-bearing for reader history or process records.

## Gates — author's word wanted

- **Typed bodies — CLOSED in dialog; folds LANDED** (the author read, corrected, and settled it live; settled.md §1 now redistributed into the specs) (`spec/research/typed-bodies.md`, written whole from the dialog; the agent exploration archived in typed-json.md): six designs D0–D5; author's "typing ≠ decomposition" confirmed (the axis is where truth lives); full decomposition (D2) charted as retired by the substrate's own rulings; **the predecessor citation resolvable today** as an asymmetric connection chunk (instance-on-citing, relates-on-cited — zero new mechanism); `attach` demoted if schema-level ref typing (D1) lands; **one `keys` spec-field ruling could close three queued opens at once** (typed refs + content-type register + well-known keys); containment preserved under D1 (declarations live in the owning type's spec) but ref-constraint naming couples to the bootstrap-ID debt. Deciding question ANSWERED in dialog (author): target-side discovery is load-bearing from the start — prose mentions are the consumer ("all the mentions are placements"); knowledge modeling is in (ownership fences ontology creep, not domain); the model is write-in-body, graph-grows-the-shadow (projection = standing `explode`), with three pinned obligations: derived-marked/no-reach, commit-time, hand-placed relates survives. See threads-dialog.md.

- **The citation shape — held open, author not ready to trust** ("all feels fresh and unsettled"): commit-pinned references as data on records (`{chunk, at-commit}`). Do not lean on it; stale-display absorbs its display purpose and collations-as-values removes its reference purpose. Revisit only if a real case demands a version-bound reference to a *mutable* chunk.
- **Agent contexts as selections — RESOLVED by E**: a context *is* a selection (the argument), a collation is `list<selection>`, so the thread face holds N-source contexts natively; piped contexts are expression elements the reader already renders.
**The batch-raised gates are CLOSED — the small-rulings cluster, 2026-08-12** (rulings in [`conclusions.md`](spec/research/arc/conclusions.md), folded in place): tile tree = typed fields, leaf = process, union refs, one-hop `[session]` · first session owned by the active project's root · frame reads `[self] | follow(owned)` · `links_modified` event-only (truth vs performance index — revisit with object-model/worlds) · existence-oracle leak accepted for v0.1, revisit at peering · archetype hop as derived admission (address + contract) · temporal queries in v0.1 scope (pilot defer deleted) · single-request derived by the planner, never typed · slot preferences reader-owned (pin = the explicit seat; shape-rules as collation settings) · context menu composes by layer · steward calls: governance at db, one `$type` encoding, projections declare ordering, `read_batch` identity tokens, host-direct return path.

Still standing:

- ~~`follow`'s parameterization~~ — **landed** (engine.md, *Hops and `follow`*; Next 3).
- **Streaming realization** — posture set (v0.1: throttled partials on main); the buffer realization open (selection.md §14.3).
- **VM containment ruling** — *restated against the ratified cut (2026-08-22)*: `runtime-vm` is not in the first pilot (capabilities declared/shown, not enforced — engine.md, pilot.md); the containment ruling now gates the VM's own landing, not the agents project. Steward's visual-inspection duty blocked on the macOS Screen Recording grant (restart terminal between sessions).

## Tracked debt

- **Code lags the redistributed specs (the alignment pass, next build phase).** The built layer implements the previous spec generation: db validates `accepts`/`required`/`propagate` and has a two-kind placement CHECK, no `current_refs`, no ownership rules; engine carries union-accepts composition, the R7 trace exemption, boundary chunks + relates walks, instance-chain reachability, `pending/completed` status names, `RunArgs.chunks`, `results_only`, scope-shaped `await`; SDK types mirror all of it. Each is now wrong by spec, correct by tests — realign spec-first with fixtures rewritten from the new law. The anchor-row bridge retirement folds into the same pass.
- **Bootstrap IDs are hand-picked.** [`substrate.md`](spec/substrate.md) says chunk IDs are "globally unique, system-generated." Bootstrap and tests use human-readable strings as a pragmatic shortcut. Aligned fix: generated IDs everywhere, path-lookup for canonical chunks (ownership makes this natural — db.md) — load-bearing since the swap.
- **Per-program React bundling (~0.6MB)** — exits are shell-injected shared runtime (ruled direction) or projected-grade render-as-data; decide in the citizens/slots era.
- **SDK `ChunkItem.body` typed as object**; telemetry bodies are bare numbers — widen when a surface consumes events.
- Engine wire doesn't emit `unresolved` yet; `useScope` error channel; probe error channel.
- `exclude` doesn't reach dimensions/edges (unruled divergence); `Includes.rank`/`snippet` declared but unbuilt.
- Sidebar's eight flagged gaps + read-tile's pinned holes → the alignment pass.

---

## Notes

**The strange (`~/git/agi/`).** The intellectual parent — loose exploration, not a source of truth. Don't reach for it to resolve questions; if the answer isn't in the specs, the specs are what need work.

**Research informing the pilot shape.** `spec/research/ui-landscape-draft.md`, [`spec/research/ui-stacks.md`](spec/research/ui-stacks.md), [`spec/research/runtimes-and-surfaces.md`](spec/research/runtimes-and-surfaces.md) (behind the `runtime: 'webview' | 'vm'` decision), and [`spec/research/cleanroom/`](spec/research/cleanroom/) (the blind derivations behind the program layer). Decisions live in the specs; research files are reference depth.
